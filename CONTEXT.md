# ProjectAMO Data Context

ProjectAMO에서 여러 기능이 함께 사용하는 항공·기상 데이터와 그 산출물을 일관된 말로 설명하기 위한 용어집이다.

## Language

**데이터 family**:
독립적으로 생성·조회되거나 독자적인 규격, 원본, 유효기간을 가져야 하는 운영 데이터의 묶음이다.
_Avoid_: 모든 중간 산출물, 개별 필드

**태그**:
데이터 family를 저장 위치나 제공자와 무관하게 식별하는 안정적인 이름이다.
_Avoid_: 파일 경로, API 주소, 규격 버전

**운영 데이터**:
기상·위험·항공자료와 이를 조합한 브리핑·알림처럼, 두 개 이상의 기능이 같은 의미로 식별하고 재사용하는 데이터다. 사용자 계정, 세션, 화면 설정, 방문 기록과 한 화면 내부의 임시 표시값은 포함하지 않는다.
_Avoid_: 모든 데이터, 전역 데이터

**데이터 카탈로그**:
운영 데이터 family마다 태그, 상태, 현재 규격, 허용 조회 조건, 시간·공간·단위 의미, 신선도·보존 정책과 접근·소유 범위를 등록한 기준 목록이다. 실제 데이터 내용이나 최신 인스턴스 목록은 담지 않는다.
_Avoid_: 데이터 상자, 최신 인스턴스 색인

**카탈로그 상태**:
데이터 family가 현재 생산·사용되는 `active`, 정의와 기능은 보존하지만 생산이 멈춘 `suspended`, 교체를 거쳐 제거할 `deprecated` 중 어디에 있는지 나타낸다.
_Avoid_: 자료 신선도, 수집 완전성

**접근 수준**:
운영 데이터 HTTP 읽기를 누구에게 허용하는지 나타내는 `public`, `authenticated`, `backend-only` 보안 경계다.
_Avoid_: 소유 범위, 화면 표시 여부

**소유 범위**:
모든 사용자가 같은 인스턴스를 보는 `global`인지, 로그인 사용자별로 저장·색인·조회가 격리되는 `principal`인지 나타낸다. `principal`의 사용자 식별값은 요청자가 보내지 않고 서버 세션에서 결정한다.
_Avoid_: 접근 수준, 조회 조건

**제공자 원본 규격**:
외부 제공자가 전달하는 데이터의 필드, 형식, 단위와 시간 의미를 ProjectAMO가 버전별로 기록한 계약이다.
_Avoid_: 표준 규격, 원본 데이터

**ProjectAMO 표준 규격**:
제공자별 형식 차이를 제거하고 ProjectAMO의 생산자와 소비자가 공통으로 사용하는 버전된 데이터 계약이다.
_Avoid_: 원본 규격, API 응답 모양

**변환 규칙**:
특정 제공자 원본 규격을 특정 ProjectAMO 표준 규격으로 바꾸는 버전된 대응 관계다.
_Avoid_: 파서, 표준 규격

**데이터 명찰**:
실제 데이터 인스턴스에 붙어 그 데이터의 태그, 표준 규격 버전, 원본 규격, 변환 규칙과 생성 이력을 식별하는 정보다.
_Avoid_: 카탈로그, 변경 게시판

**데이터 상자**:
데이터 명찰인 `meta`와 실제 내용인 `data`를 함께 보존하고 전달하는 운영 데이터의 공통 외형이다.
_Avoid_: 내용물만 있는 기존 API 응답, 데이터 카탈로그

**표현 역할**:
같은 family의 자료가 제공자 원본, ProjectAMO 표준, 파생 결과 또는 정적 참조자료 중 어떤 역할인지 나타내는 `representation` 값이다.
_Avoid_: 규격 버전, 조회 view

**조회 view**:
같은 태그와 표현 역할 안에서 index와 field처럼 반환 모양이 둘 이상일 때만 사용하는 카탈로그 등록 구분값이다. 각 view는 별도 규격과 필수 조회 조건을 가지며 자유 입력할 수 없다.
_Avoid_: 표현 역할, 처리 단계 태그

**인스턴스 경계**:
데이터 상자 한 건에 무엇을 담는지 정한 카탈로그 규칙이다. 예를 들어 공항별 관측 묶음, 레이더 frame 목록, 모델 run의 특정 예보시간·고도 field를 구분한다.
_Avoid_: 파일 하나당 무조건 하나의 인스턴스, 레코드 ID

**일회성(`ephemeral`) 결과**:
로그인 없이 요청마다 계산하고 재사용·공유하지 않는 브리핑 결과다. 표준 규격 검증과 데이터 상자는 적용하지만 저장·최신 색인·공개 조회·보존에는 넣지 않는다.
_Avoid_: 운영 데이터 저장, 최신 인스턴스

**데이터 인스턴스**:
태그, 규격 버전, 제공자 식별정보와 실제 내용으로 고유하게 식별되는 불변의 운영 데이터 한 건이다.
_Avoid_: 데이터 family, 수집 시도

**인스턴스 ID**:
태그, 규격 버전, 표현 역할, 적용되는 view, 조회 조건·제공자 식별키와 내용 hash를 정규화해 만든 특정 데이터 인스턴스의 전역 주소다. 내용이 같아도 태그나 조회 의미가 다르면 다른 ID를 가진다.
_Avoid_: 내용 hash, 레코드 ID

**레코드 ID**:
여러 항목이 들어 있는 데이터 인스턴스 안에서 다른 기능이 독립적으로 참조하는 항목의 자연 식별자다. 독립 참조되지 않는 필드, 격자 셀, 이미지 픽셀에는 부여하지 않는다.
_Avoid_: 인스턴스 ID, 모든 필드의 ID

**제공자 식별키**:
관측시각, 모델 실행시각, 예보시간, 고도처럼 제공자 자료를 구분하는 자연 식별정보의 조합이다.
_Avoid_: 인스턴스 ID, 내용 hash

**내용 hash**:
데이터 인스턴스의 정규화된 실제 내용에 대해 계산한 SHA-256 값으로, 내용 동일성과 변경 여부를 판단하는 지문이다. 인스턴스 ID를 만드는 재료지만 태그·버전·조회 의미를 포함하지 않으므로 인스턴스 ID 자체는 아니다.
_Avoid_: 인스턴스 ID, 제공자 식별키

**상위 데이터 참조**:
파생 데이터가 직접 사용한 상위 데이터 인스턴스의 태그와 인스턴스 ID 연결이다. 전체 조상을 복사하지 않고 참조를 단계적으로 따라간다.
_Avoid_: 원본 내용 복사, 모든 조상의 반복 기록

**데이터 출판**:
생산된 운영 데이터를 등록된 규격으로 검증하고 데이터 명찰과 함께 다른 기능이 사용할 수 있는 상태로 만드는 행위다. 모든 운영 데이터 생산자는 같은 출판 규칙을 따른다.
_Avoid_: 파일 저장, API 응답

**최신 인스턴스 색인**:
각 데이터 태그가 현재 가리키는 최신 정상 인스턴스의 ID, 내용 hash, 규격 버전과 시각을 모은 변경 게시판이다. 모든 출판 순서를 기록하는 이력은 아니다.
_Avoid_: 출판 이력, 데이터 카탈로그

**태그 기반 조회**:
소비자가 파일 위치나 제공자별 저장 방식을 알지 않고 데이터 태그와 필요한 조건으로 운영 데이터를 요청하는 방식이다.
_Avoid_: 파일 읽기, 저장소 직접 접근

**조회 조건**:
같은 태그 아래 여러 인스턴스 중 필요한 자료를 고르는 값이다. 국내·해외 범위, 공항, 모델 실행시각, 예보시간, 고도 등이 이에 해당하며 태그 이름에는 포함하지 않는다.
_Avoid_: 태그 조각, 저장 경로

**규격 전진 변환**:
저장된 데이터 인스턴스를 수정하지 않고, 명시적인 family별 규칙을 이용해 구버전 표준 규격을 신버전 표준 규격으로 읽는 변환이다. 최초 v2에서 실제 호환 필요가 생길 때 해당 변환을 함께 도입하며, v1만 있는 동안 범용 registry를 미리 만들지 않는다.
_Avoid_: 원본 덮어쓰기, 자동 추측

**규격 버전**:
데이터의 필드, 단위, 필수 여부 또는 의미가 달라질 때마다 증가하며 이전 정의를 덮어쓰지 않는 정수 번호다.
_Avoid_: 인스턴스 ID, 변환 규칙 버전

**보존 정책**:
데이터 family별로 제공자 원본과 표준 데이터의 보존 방식과 한도를 각각 정한 규칙이다. 규격과 변환 정의는 과거 데이터를 읽을 수 있도록 계속 보존한다.
_Avoid_: 모든 데이터에 적용하는 단일 보존 기간

**수집 완전성**:
한 번의 수집에서 기대한 항목이 모두 새로 확보됐는지 나타내는 `complete` 또는 `partial` 상태다.
_Avoid_: 자료 신선도, 규격 검증 결과

**레코드 상태**:
수집된 항목이 이번에 확보한 `current`인지, 부분 실패 때문에 이전 정상값을 이어 쓴 `carried_forward`인지 나타낸다.
_Avoid_: 자료 신선도, 데이터 유효성

**자료 신선도**:
데이터 시각과 family별 허용시간을 현재 시각과 비교해 계산한 `fresh`, `stale`, `expired` 상태다.
_Avoid_: 레코드 상태, 수집 완전성
