# RF-002-W11 / A09 관리자·시연·운영 도구 진단

- 기준: main HEAD `075eb89a8a2ffe5fde8ccf9bf1cbec954284fa41`, 기존 AGENTS/CLAUDE/docs/README/docs/refactoring 미커밋 보존.
- 허용: 소스·정책·기존 산출물 읽기 및 본 W11.md만 작성. 시연·계정·외부 알림 조작, 서버·수집·검사·브라우저·성능 실행, 추가 에이전트 없음.
- A01: 2248 tracked 중 A09 48파일. 대표 source graph 601/1297, 금지 import/SCC0은 분류 입력이며 런타임 안전성·미사용 증명 아님.
- 입력: AGENTS, tasks W11/반환 형식, progress, Architecture, data-and-time/entry-sequences/검증 계약, W03와 audit/common.md, report RF-100/RF-118.
- coordinator msg_acf41c480e7d: admin 30사례 기존 통과는 기본 콘솔 계약만 보호하며 신규실행 금지. 최종 경로는 최초 배정의 artifacts/refactoring/S004/W11.md를 사용한다.

## 중간 체크포인트 CP1

- /api/admin 전체는 requireRole(admin), /api/forecaster는 역할+DB 담당공항, /api/dev는 DISABLE_COLLECTION mount+requireAuth이다. test-only 계정 역할 변경은 의도적이며 production 비노출 조건은 추가 대조 중이다.
- /dev standalone은 DEV+health.testMode를 보지만 sidebar DeveloperConsole은 production admin에도 노출되고 동일 Trigger/Observe 탭을 재사용한다. 모달 경로의 dead API/error 표현을 확인 중이다.
- demo-session start/stop은 active symlink+cache reload, capture만 drain/save. admin router에는 구형 live-backup/cron정지 주석이 남았다. snapshot 같은 이름 재저장과 active revision의 연결을 좁게 추적 중이다.
- health는 active data 내용과 live stats/실제 now를 합친다. freshness·collector·API outcome이 별개인 구조는 유지하되 UI 의미/실패 후 stale 상태를 확인 중이다.
- DB VACUUM 백업→그 DB가 참조한 기관 첨부 복사/해시 manifest는 보존할 구조. 최신백업 표시는 파일명/mtime만 확인한다.
- 다음: snapshot readiness/overwrite, 백업 publication/restore, metrics·visits·forecaster 실제소비, 전체48파일 원장과 후보·좁은 후속 질문.

## 중간 체크포인트 CP2

- 2026-09-12 01:43 KST: A09 48파일을 역할·진입점 기준으로 분류했고 admin/forecaster/dev 및 실제 server mount를 대조했다. production+DISABLE_COLLECTION에서도 requireAuth-only dev 역할 변경 경로가 등록되는 조건을 확인했으며 실제 운영 설정·계정 요청은 하지 않았다.
- snapshot overwrite/자동 이름 할당은 capture drain 경계와 active symlink/revision을 함께 보아야 한다. DB 백업은 VACUUM→참조 첨부+해시 manifest가 장점이며 파일명/mtime 기반 완료 판정은 중단 복구 검증이 남는다.
- 운영 health의 active 자료/live stats 혼합, 조회 실패 후 녹색 유지, 낙뢰 공항 수의 발효 건수 표시, 감시 시간창과 실제 사용자별 선택/시연 중지의 의미 차이를 후보로 정리했다.
- ops alerts는 production 및 ALERTS_DISABLED 조건을 별도로 확인했다. DISABLE_COLLECTION만으로 DB 백업·운영 알림·직접 tick의 부작용이 차단된다고 볼 수 없다.
- 남은 작업: 후보별 선행·검증/유지 구조, 전체 파일 원장, 미확인 좁은 질문을 최종 보고서로 합친다. 애플리케이션 실행 검증은 모두 미실행이다.

## 최종 반환 — 독립 조사 1묶음, A09 부분 확인

**결론:** 관리자 역할 검사와 실황/시연 포인터 소유 구조는 유지할 가치가 있다. 우선 좁혀야 할 것은 수집 중지 플래그를 테스트 권한으로 겸용하는 mount 조건(W11-01), 시연/주입의 데이터·시각 경계(W11-04/05), 운영 화면이 상태를 요약하면서 의미를 바꾸는 경로(W11-03/06/07/08)다. 크기·파일명·대표 import graph만으로 분리·삭제를 제안하지 않는다.

**판정 범위:** A09 48개 추적 파일의 역할/진입점/직접 소비자를 분류하고 위험별 대표 흐름을 소스로 추적했다. 모든 분기·실운영 배치·장시간 동작을 검증한 것은 아니므로 A09 전체 완료 선언은 하지 않는다. 아래 `F`는 소스에서 확인한 사실, `H`는 조건을 만족할 때 예상되는 결과 또는 개선 가설, `U`는 이번에 실행하지 않은 검증이다. 기존 실행 결과는 출처를 별도로 적는다.

### 1. 전체 파일 원장

경로는 저장소 기준이다. `추적`은 해당 파일의 주요 동작과 호출자/소비자를 읽었다는 뜻이며 전 분기 실행 증명이 아니다. `경계`는 선언·사용 지점 확인에 그치며 렌더링/세부 알고리즘 검증이 남는다. 표의 B=`backend/src/`, F=`frontend/src/features/`.

| 파일 | 심볼·시작 줄 | 자원·직접 소비자 / 확인 깊이 |
| --- | --- | --- |
| B/admin/router.js | createAdminRouter:26 | server mount → admin API, 권한·시연·계정·운영 조회; 추적 |
| B/admin/data-health-catalog.js | CATALOG:43 | 출처/성격/정상 주기/quiet/disable 규칙 → readDataHealth; 추적 |
| B/admin/data-health.js | readDataHealth:71 | active 내용·live stats·실제 now → admin/ops alerts; 추적 |
| B/admin/freshness.js | isQuiet:13, judge:29 | 시각 주입 가능한 순수 판정 → health; 추적 |
| B/admin/alert-watches.js | watchStatus:10, listAlertWatches:23 | DB 비행/구독 → 감시 목록; 추적 |
| B/admin/metrics.js | currentResources:9, startSampler:36 | OS·statfs → DB 60초/7일 시계열 → 화면/디스크 예측; 추적 |
| B/admin/disk-forecast.js | forecastDiskFull:4 | 기간 양끝의 양의 증가량으로 소진 일자 추정; 추적 |
| B/admin/disk-usage.js | readDiskUsage:22 | 실황 루트 하위 용량, 프로세스 내 5분 캐시 → server-health; 추적 |
| B/admin/process-health.js | recordBoot:14, processHealth:29 | 실황 루트 재시작 파일·uptime/heap → 화면/ops; 추적 |
| B/admin/deployment.js | certificateExpiry:12, deploymentInfo:50 | 인증서·환경 commit/로컬 git·배포표식 → server-health; 추적 |
| B/admin/visits.js | recordVisit:9, trafficStats:31 | 방문 쿠키·SQLite 일/시간 집계 → traffic/UsersScreen; 추적 |
| B/admin/trends.js | bucketByDay:19, visitTrend:28 | DB 일별 자료 → 주/월 묶음/신규방문/가입자; 추적 |
| B/admin/db-backup.js | backupDatabase:60, startDailyBackup:93 | VACUUM·일일 cron·7개 보관·최근백업 → 서버/UI; 추적 |
| B/admin/organization-backup-files.js | backupOrganizationFiles:19, restoreOrganizationBackup:40 | 백업 DB 참조 첨부·해시 manifest·새 경로 복원 → backup/CLI; 추적 |
| B/dev/data-view.js | createDataViewManager:45 | active symlink·뷰 메타·명시적 live 통과 → demo-session/서버; 추적 |
| B/dev/demo-session.js | createDemoSession:7 | inspect/start/stop/capture + active cache reload; 추적 |
| B/dev/demo-mode.js | getEffectiveNow:11, recordDemoEvent:31 | 현재 뷰의 유효시각·50개 메모리 로그 → 앱/운영 라우터; 추적 |
| B/dev/snapshot-store.js | saveSnapshot:152, loadSnapshot:174, inspectSnapshot:231 | 캡처 staging/레거시 복원/준비 검사 → 관리자·테스트; 추적 |
| B/dev/scenario.js | createDevRouter:125 | 로그인된 테스트 사용자 주입/reset/tick/role/관찰 → dev UI; 추적 |
| B/dev/instrument.js | recordRequest:8, aggregateByPath:18 | API 종료 로그 500개·캐시 카운터 → 관찰; 추적 |
| B/forecaster/router.js | createForecasterRouter:15 | 담당공항 문의 큐/현재 경로/claim/close → 테스트, 프론트 실사용 미발견; 추적 |
| F/admin/AdminPage.jsx | AdminPage:8 | AuthContext admin 화면 차단 → AdminShell; 추적 |
| F/admin/AdminShell.jsx | AdminShell:32 | 7개 메뉴·기본 4요청/5초·공유 range → 화면; 추적 |
| F/admin/adminApi.js | getMetrics:13 등 | credentials 포함 GET/POST 및 오류 → admin와 DemoPanel; 추적 |
| F/admin/DemoModePanel.jsx | DemoModePanel:31 | status/list/log 5초·save/load/revert·전환 event → TriggerTab만 직접 사용; 추적 |
| F/admin/CreateForecasterDialog.jsx | CreateForecasterDialog:18 | 입력/busy/오류·생성 POST → AccountsScreen; 추적 |
| F/admin/AdminPage.css | .admin-page:8 | 관리자 scope 토큰/그리드/모달/반응형 → 관리자/시연 UI; 경계, W12 |
| F/admin/components/AttentionList.jsx | whyLine:8, AttentionList:22 | health 이상행·경과시각·메뉴 이동 → Overview; 추적 |
| F/admin/components/DataGrid.jsx | Tile:22, DataGrid:42 | 출처/성격별 타일·activeCount 문구 → Overview; 추적 |
| F/admin/components/Chart.jsx | Axes:11, LineChart:44, GroupedBarChart:118 | 축/hover/heatmap·SVG → Overview/Server/Users; 경계 |
| F/admin/lib/adminFormat.js | STATUS_WORD:6, formatAge:39 | 상태·단위·비율·trendGroups → 화면 공통; 추적 |
| F/admin/lib/apiOperationSummary.js | apiOperationSummary:3 | 개별 operation 결과/다음 예정 → DataCollection; 추적 |
| F/admin/lib/chartAxis.js | axisTicks:12, plotGeometry:18 | 순수 축 계산 → Chart 및 단위 테스트; 추적 |
| F/admin/lib/menus.js | MENUS:5, topSignals:28 | 7메뉴·상단 4신호·배지 → AdminShell; 추적 |
| F/admin/screens/AccountsScreen.jsx | AccountsScreen:14 | users 1회·승인/거절·생성 후 reload → admin API; 추적 |
| F/admin/screens/AlertWatchScreen.jsx | AlertWatchScreen:21 | 진입/수동 갱신·상태/구독 표시 → alert-watches; 추적 |
| F/admin/screens/ApiExecutionDialog.jsx | ApiExecutionDialog:4 | row operation 상세 모달 → DataCollection; 추적 |
| F/admin/screens/ApiUsageScreen.jsx | ApiUsageScreen:14 | API key 사용/차단·온디맨드 5초 → api-hub-usage; 추적 |
| F/admin/screens/DataCollectionScreen.jsx | DataCollectionScreen:13 | health props·모델 가용/수집/실행시각·통계 → 상세; 추적 |
| F/admin/screens/OverviewScreen.jsx | OverviewScreen:16 | health/server/metrics → 신호/이상/차트; 추적 |
| F/admin/screens/ServerResourceScreen.jsx | Gauge:17, RANGES:39 | OS 지표·period·디스크/인증서/백업 표시; 추적(세부 SVG는 경계) |
| F/admin/screens/UsersScreen.jsx | UsersScreen:18 | traffic 5초·기간별 trends·heatmap; 추적 |
| F/developer/DeveloperConsoleButton.jsx | DeveloperConsoleButton:13 | DEV test 또는 admin 노출·lazy modal·개발 자동 로그인; 추적 |
| F/developer/DeveloperConsole.jsx | DeveloperConsole:15 | open/탭 수명 → Trigger/Observe, runtime test gate 없음; 추적 |
| F/developer/DeveloperPage.jsx | DeveloperPage:16 | DEV route + health.testMode → 동일 탭; 추적 |
| F/developer/developerApi.js | getHealth:11 등 | dev/me/admin 관련 API adapter → 두 탭; 추적 |
| F/developer/tabs/ObserveTab.jsx | ObserveTab:28 | 6요청/2초·alive cleanup·관찰 표; 추적 |
| F/developer/tabs/TriggerTab.jsx | TriggerTab:23 | route 선택·가상 주입·tick·역할·push·DemoPanel; 추적 |

다른 영역은 계약 경계만 읽었다: `backend/server.js`, `backend/src/index.js`, `store.js`, `stats.js`, `auth/middleware.js`, `db/users.js`, `alerts/scheduler.js`, `alerts/ops-alerts.js`, `processors/lightning-processor.js`, `backend/restore-organization-backup.js`, `frontend/src/app/App.jsx`, `shared/timezone/TimeZoneContext.jsx`, `shared/demoMode/useDemoMode.js`, 관련 테스트·문서. W03 생산자 7경로 및 W04 공통 구독 전체 재조사는 하지 않았다.

### 2. 실제 진입·권한·플래그

| 진입 | 실제 gate | 데이터·부작용/소비자 |
| --- | --- | --- |
| `/admin` | App:401 AuthProvider → AdminPage:11 admin role UI | 7메뉴; API 권한은 별도로 server에서 검사 |
| `/api/admin/*` | server:277 → router:29 requireRole(admin) | 운영 조회, 계정 승인/거절·예보관 생성, snapshot/capture/start/stop |
| `/api/forecaster/*` | server:276 → forecaster/router:19 requireRole(forecaster), 관리자 role 허용 후에도 DB 담당공항 필터(:9/:29) | 문의 목록/상세/claim/close; admin도 담당공항 없으면 빈 목록 |
| `/dev` | App:404 DEV build + DeveloperPage:22 health.testMode | 두 탭; 이 화면의 gate 자체가 API 권한은 아님 |
| 메인 sidebar 개발자 모달 | DeveloperConsoleButton:31 `(DEV && testMode) || admin` | 운영 admin에도 의도적으로 노출, 동일 Trigger/Observe 사용 |
| `/api/dev/*` | server:257 NODE_ENV != test, :278 DISABLE_COLLECTION truthy → scenario:128 requireAuth | 글로벌 active cache 주입, 자기 알림 정리/역할 DB 변경, 전체 사용자 tick, 관찰 |
| `/api/health` | server:1134 공개 | `ok:true`/uptime/testMode; 수집·DB readiness 판정 아님 |
| `/api/demo-mode` | server:1137 공개 | active on/name/effectiveNow/revision → 지도 배지·시각 구독 |

`requireRole`은 세션 역할을 확인하며 admin을 상위 역할로 허용한다(`auth/middleware.js:17`). 세션 만료는 실제 시각이다. 계정 승인/거절의 기존 세션 반영 문제는 W10 소유이며 이 보고서에서 별도 해결책을 확정하지 않는다. 예보관 생성은 admin API가 role=forecaster/status=active를 강제하고 DB createUser가 비밀번호/사용자명·공항 목록을 검증한다(`admin/router.js:159`, `db/users.js:14`). 승인/거절 API는 ID와 존재를 검사하지만 pending 상태만 수정하도록 제한하지는 않는다(:76); UI의 pending 목록과 서버의 변경 가능한 상태 범위를 같다고 가정하지 않는다.

| 실행 조건 | 수집·조회 | 별도 동작 |
| --- | --- | --- |
| NODE_ENV=test | server의 세션/업무 라우터/listen 블록 생략 | 라우터 단위 테스트가 직접 만든 Express 앱과 실제 mount 조건이 다름 |
| NODE_ENV!=test, DISABLE_COLLECTION 없음 | 초기/cron 수집 시작, readLatest disk 재조정 | admin/forecaster 등록, dev 미등록 |
| NODE_ENV!=test, DISABLE_COLLECTION truthy | index:282 수집 시작 조기 반환, server:288 active cache 직접 반환 | dev 등록, 요청 관찰 켜짐(:232), metrics/boot/DB backup/개인 알림 scheduler는 별도 시작 |
| production + DISABLE_COLLECTION truthy | 위와 동일 | dev 등록이 여전히 가능; 운영 알림은 production 및 ALERTS_DISABLED 별도 조건 |

문자열 환경변수는 truthy 판정이므로 `'0'`도 같은 분기에 들어간다. DB 백업은 `DB_BACKUP_DISABLED`(`db-backup.js:94`), ops 알림은 `ALERTS_DISABLED || NODE_ENV !== 'production'`(`alerts/ops-alerts.js:108`)가 각각 결정한다. `server.js:1383` 기관 평가와 `alerts/scheduler.js:214` 개인 자동 tick은 demo를 건너뛰지만 DISABLE_COLLECTION 자체가 모든 부작용을 차단하지 않는다. 온디맨드 ADS-B 조회의 별도 갱신 경계는 W03 입력이며 다시 실행하지 않았다.

### 3. 실황·시연·주입과 시각 흐름

1. **실황:** 수집기는 `config.storage.base_path`에 게시하고 활성 뷰는 `.active-data`다. `data-view.js:84`가 스냅샷별 자료 링크와 `.view.json`의 mode/name/referenceTime/revision을 만든 뒤 active symlink를 전환한다. AIP·태풍·지형 live 통과(:7/:113)는 현재 명시된 예외이며 누락 fallback과 구별한다.
2. **시연 시작:** TriggerTab → DemoModePanel → adminApi load → admin/router:124 → demoSession.startDemo:36 → inspectSnapshot → activateDemo → initActiveFromFiles. 시작 자체는 외부 호출·drain·복사·실황 백업을 하지 않는다. 실패 시 이전 뷰 복구(:60)를 시도한다. 종료(:69)는 계속 수집된 최신 실황으로 전환하고 active cache를 읽는다.
3. **시각:** `demo-mode.js:11 getEffectiveNow`는 활성 뷰 기준이다. briefing/선택시각/공개 `/api/demo-mode`(server:1137)가 이를 소비한다. 서버 자원·세션 TTL·DB 백업·운영 방문 집계는 실제 시각이 맞으므로 모든 Date.now를 바꾸면 안 된다. 시연 기상·경로 판정과 운영시계의 경계를 명시해야 한다.
4. **브라우저:** DemoPanel:59는 mutation 응답 후 같은 window의 `projectamo:data-view-changed`를 발행한다. 메인 안 모달은 이 event를 공유하지만 별도 창에는 자동 전파되지 않는다. `shared/demoMode/useDemoMode.js`는 별도 30초 `/api/demo-mode` 구독이며 revision을 상태로 유지하지 않는다. 공통 자료 구독과 유효시각 갱신 세대 차이는 RF-118/W04 입력에 연결한다; 지연 실제 측정은 하지 않았다.
5. **캡처:** admin/router:107 자동 이름 결정 → demoSession.captureSnapshot:31 drain → snapshot-store.saveSnapshot:152 staging → 게시. full-dir 7종(`kim_nwp/ktg/radar/satellite/airport_model_comparison/metar/amos`)은 참조 payload·최근 이력 보존 이유가 있다. 나머지는 latest 중심이다. 준비 검사는 21종/JSON/참조/레이더36·위성18/ADSB 30분을 요구한다. KIM/KTG는 index 경로 존재까지이며 모든 격자 payload 의미 검증을 뜻하지 않는다. 이후 추가 자료가 전부 필수라는 뜻도 아니다.
6. **가상 주입:** scenario:131 자기 경로 소유 확인 후 공용 active cache에 overlay를 넣고 실제 알림 행·발송 경로를 사용한다(:146/:178). reset은 live base의 일부 타입을 cache에 다시 넣는다(:188); tick은 real now로 전체 사용자 DB 경로를 평가하지만 브리핑 자료 now는 effectiveNow다. 따라서 1인 테스트 인스턴스라는 전제가 데이터/권한/발송 전체에 걸린다.

**기존 결과와 연결:** `docs/refactoring/report.md:26` RF-100과 `audit/common.md:33`에는 주 담당의 독립 임시 fixture 재현으로 airport_info 전체 upstream 실패 시 이전 live 대신 demo가 live fallback에 저장된 근거가 있다(`artifacts/refactoring/S004/repro-data-view.json`). W11은 그 실행을 반복하지 않았으며, pointer 전환 소유를 바꾸는 것으로 RF-100이 자동 해결된다고 보지 않는다.

### 4. 운영·백업·예보관 소비 흐름

- **Health:** admin/router:41 active_path + store.getCached + live stats → data-health의 내용시각/last_success/freshness/collectorExecution/apiProblems → AdminShell 상단 4신호·Overview·DataCollection·ApiUsage. `alerts/ops-alerts.js:49 collectState`도 같은 health를 받아 운영 알림 판단에 쓴다. HTTP liveness, 자료 신선도, 수집기 실행, API 개별 결과는 다른 질문이므로 현재의 분리된 등록부를 유지한다.
- **Metrics:** server 시작 → startSampler(즉시+60초) → SQLite metrics(7일 보관) → `/metrics?range=1h|24h|7d` → 차트. `/server-health`는 live 디렉터리 용량(5분 캐시), 최근 실패20개, 7일 디스크 예측, 프로세스 상태, 인증서/commit/배포표식, 최근 DB 백업을 합친다. `deployment.js:20` 환경 commit이 없으면 읽기용 git 프로세스를 호출한다; 이 보고서는 해당 API도 실행하지 않았다.
- **Visit:** visitTracker는 페이지 GET 및 `/api/auth/me` 방문을 기록한다(`visits.js:43`). 5분 내 마지막 방문을 online으로 세며 실제 연결 수와 같지 않다. visits 보존90일, 일/시간 집계400일; 일별 unique는 UTC 날짜, heatmap은 KST 날짜/시간이다(:13/:14). trends는 14일/8주/약6개월을 소비하므로 retained visitor와 전체 역사도 구별해야 한다.
- **DB backup:** server:1379 → startDailyBackup(오늘 없음/03:10 KST) → VACUUM INTO → **그 백업 DB가 참조하는** 기관 원본/썸네일 복사 → 각 파일·DB SHA manifest → 성공 후 keep7 정리. 운영 DB와 첨부 root를 각각 현재 상태로 따로 복사하지 않는 구조가 장점이다. 복원은 `backend/restore-organization-backup.js:1` → restoreOrganizationBackup:40 → 해시 검증 → 존재하지 않는 새 DB/첨부 경로 기록. 운영 프로세스의 실제 전환은 문서상 별도 유지보수 단계이고 이번에 하지 않았다.
- **예보관:** pilot 문의 생성 계약(W10/W06) → requests DB → forecaster role+DB 담당공항 → 목록/상세/claim/close. 상세는 문의 당시 고정본이 아니라 현재 routes.payload를 조회한다(:51). frontend에서 `/api/forecaster` 직접 소비자를 찾지 못했고 `requests-flow.test.js` 및 제안서가 소비 계약을 설명한다. `docs/design/proposals/2026-07-05-STATUS-implementation-order.md:47/:58`은 백엔드 완료/프론트 다음 단계임을 명시하므로 삭제할 미사용 코드라고 판정하지 않는다.

### 5. 타이머·요청·상태 수명 목록

| 소유자 | 시작/전환 | 정리·남는 경계 |
| --- | --- | --- |
| AdminShell:40 | mount/range 변경 즉시 + 5초, 4개 allSettled | interval 해제; in-flight 취소·세대·조회 실패 상태 없음 |
| UsersScreen:23/:30 | traffic 5초, trends는 granularity 변경 | interval 정리; 오래된 trends 응답 방어/오류 표시 없음 |
| ApiUsageScreen:18 | mount + 5초 | interval 정리, 이전 요청 취소 없음 |
| AlertWatchScreen:21 | mount + 수동 갱신 | 자동 폴링 없음은 현재 의도; 오류 표시는 존재 |
| AccountsScreen:18/:21 | mount + mutation 이후 users/onChanged | 승인/거절 busy·오류 상태 부족; CreateForecasterDialog는 busy/catch 있음 |
| DemoModePanel:41/:49 | status/list/log 5초 | timer 정리; stale 응답/실패 상태 경계 없음 |
| DemoModePanel:54 | 실행 중 elapsed 1초 + mutation | finally에서 timer 해제; 패널 unmount 자체의 실행 timer/요청 취소 없음 |
| DeveloperConsole:29 | open일 때 탭 mount, 닫으면 반환 null | 탭 언마운트로 interval 정리; API capability gate는 없음 |
| DeveloperConsoleButton:19/:25 | DEV health 1회·조건부 자동 로그인 1회 | modal lazy 렌더는 visible 시 이미 수행(:49); 실제 열기 전 import 안 된다는 주석과 불일치 가능, 성능 미측정 |
| ObserveTab:28 | mount + 2초, 6개 allSettled | alive guard+interval 해제 있음; abort/in-flight 단일화 없음, 실패는 빈 상태처럼 보임 |
| metrics:36 | 프로세스 시작 즉시 + 60초 | unref + 해제함수 제공; server가 반환 해제함수는 보관하지 않음(프로세스 수명) |
| db-backup:93 | 오늘 백업 없음 + 03:10 KST cron | 핸들 반환, server에서 보관하지 않음; DISABLE과 독립 |
| ops alerts:107 | production/ALERTS_DISABLED gate + 5분 cron | 별도 발송 gate; demo 스킵 없음 |
| instrument:4 / demo-mode:28 | 프로세스 API finish / 시연 action | 500/50개 메모리 제한 있음, 재시작 시 사라짐; 영구 감사 로그라고 부르지 않음 |

프로세스 수명의 timer를 반환값 미사용만으로 누수라고 판정하지 않는다. 브라우저 폴링 문제는 endpoint별 권한/실패 의미와 공통 요청 소유 계약(W04)을 정한 뒤 적용해야 한다.

### 6. 후보별 근거·효과·위험·선행·필요 검증

우선순위는 이번 조사자의 제안이며 전역 RF 작업 선택은 주 담당이 한다. 아래 검증은 모두 **U: 미실행**, 운영 데이터 대신 별도 임시 fixture/모의 발송기를 전제로 한 다음 단계다.

#### W11-01 — 수집 중지와 테스트 변경 권한을 분리 (P1, 조건부 권한 위험)

- **F/원인:** `backend/server.js:257/:278/:280`의 dev mount는 NODE_ENV!=test와 DISABLE_COLLECTION truthy뿐이다. `backend/src/dev/scenario.js:128`은 requireAuth, :239 role endpoint는 허용 role을 받아 DB(:244)와 현재 세션(:245)을 수정한다. :238의 “운영엔 존재하지 않음” 주석은 이 mount 조건으로 보장되지 않는다. production admin 콘솔을 허용하는 UI 계약과 별개의 서버 권한 문제다.
- **H/영향:** production에서 수집을 멈추려고 해당 플래그를 켜면 일반 로그인 사용자가 자기 역할을 admin으로 바꿀 수 있는 경로가 등록된다. 실제 운영 플래그 상태나 공격 발생을 확인한 것이 아니다. role 변경은 DB 영구 갱신이라 주석의 “임시”가 자동 rollback을 뜻하지 않는다.
- **제안/효과:** 테스트 변경 API의 명시적 실행 모드/DB 격리 조건과 non-production 서버 gate를 수집 실행 여부에서 분리한다. 읽기 관찰·운영 시연 capability도 구분하면 플래그 의미가 검토 가능해진다.
- **유지/위험/선행:** requireRole admin 및 실제 시연 admin API는 유지한다. 테스트 인스턴스의 1인 역할 전환 계약·실행 스크립트(W02)와 full-server mount matrix를 먼저 고정한다. UI 숨김만으로 해결하거나 전체 운영 콘솔을 제거하면 승인된 시연 경로가 사라진다.
- **U:** 별도 DB에서 production/development/test × flag 없음/1/0 × 익명/pilot/admin으로 실제 mount/401/403/404를 검증한다. production+flag가 있어도 dev mutation이 등록되지 않으며 테스트 설정에서만 역할 실습이 되는지 확인한다; 운영 계정 요청은 금지.

#### W11-02 — 생산용 관리자 콘솔의 capability 표시 (P2)

- **F/원인:** `DeveloperConsoleButton.jsx:31/:49`는 운영 admin에도 모달을 렌더한다. `DeveloperConsole.jsx:15`에는 DeveloperPage:22의 runtime testMode gate가 없고 `tabs/TriggerTab.jsx:116`은 DemoPanel과 test 전용 주입/role/tick을 같은 탭에서 제공한다. Observe도 dev endpoint와 me/meta를 함께 조회한다. 실제 backend dev 미등록 일반 운영에서는 그 dev 요청만 404가 된다.
- **H/영향:** 정상 운영 시연과 실행 불가능한 테스트 동작이 같은 조작 화면에 섞이며 관찰의 빈 값이 정상으로 읽힐 수 있다. `/dev` route의 build gate를 전체 콘솔 경로의 gate라고 설명하면 잘못된 안전 가정이 된다.
- **제안/효과:** 서버가 보장하는 기능별 capability에 맞춰 운영 시연, 자체 알림 점검, 테스트 주입, 관찰을 표시한다. 실제 entry contract를 하나의 명시적 모델로 정리하되 두 페이지를 무조건 합칠 필요는 없다.
- **유지/위험/선행:** 운영 admin DemoPanel·인증된 push 점검은 의도된 소비자다. W11-01의 서버 정책을 먼저 정하고 UI가 그 권한을 대체하지 않도록 한다. 파일이 developer 폴더에 있다는 이유로 production 번들에서 전부 제거하면 안 된다.
- **U:** 일반 운영 admin 모달에서 지원되는 동작만 사용 가능하고 미지원 API 폴링이 없는지, DEV+test와 일반 DEV 각각의 탭/오류를 검증한다. 외부 push는 mock만 사용한다.

#### W11-03 — 운영 상태의 조회 실패·요청 세대 표시 (P1)

- **F/원인:** `AdminShell.jsx:40` 4요청의 성공값만 갱신하고 실패는 저장하지 않는다. `lib/menus.js:28`은 null health/server를 0문제로 해석해 ok 신호를 만든다. :53은 interval만 해제한다. `UsersScreen.jsx:30` granularity 요청과 AdminShell range 요청에는 이전 응답을 배제할 세대 조건이 없다. DemoPanel:41도 조회 실패를 숨긴다.
- **H/영향:** 최초 오류에는 “불러오는 중”과 녹색이 공존하고, 이후 인증만료/500에는 마지막 정상 상태가 계속 보인다. 느린 이전 range/granularity 응답이 새 선택을 덮을 수 있다. 마지막 자료 보존 자체는 필요한데 성공·실패·조회시각을 합쳐 버리는 것이 원인이다.
- **제안/효과:** 마지막 성공 payload를 보존하면서 조회 상태/lastSuccess/request key를 분리하고 현재 세대만 commit한다. 기본 polls의 소유자와 화면별 추가 요청을 W04 계약에 맞춘다.
- **유지/위험/선행:** 부분 성공을 버리는 Promise.all 전환이나 모든 실패에 데이터 삭제는 피한다. endpoint별 401/403/5xx UX와 공유 polling 적용 범위를 먼저 정한다. 단순 hook 이동은 race를 그대로 옮길 수 있다.
- **U:** 첫 요청 실패, 정상→401/500, A 10초/B 1초 역순 응답, range 변경·언마운트·재진입 fixture로 색/문구/마지막 성공시각/요청수를 확인한다. 의미 있는 기존 admin 계약 확장은 W01/주 담당이 실행한다.

#### W11-04 — 테스트 overlay/reset/tick의 자료·시각·사용자 범위 (P1)

- **F/원인:** `scenario.js:65` injectAirport는 metar/taf 국내·해외 cache를 바꾼다. reset의 INJECT_TYPES(:29)는 국내 metar/taf/sigmet/notam뿐이고 :189는 active가 아닌 live base를 읽으며 파일이 있을 때만 복구한다. `scenario.js:202` 직접 tick → `alerts/scheduler.js:182`는 real now로 cleanup/사용자별 비행 선택을 하고 :174의 재브리핑은 effectiveNow다. 자동 tick의 demo skip(:214)은 직접 tick에 적용되지 않는다. inject도 실제 알림 DB/발송 adapter를 호출한다(:146/:178).
- **H/영향:** 해외 METAR/TAF 주입이 reset 뒤 남거나 demo pointer/기준시각은 그대로인데 일부 active cache가 live 값으로 바뀔 수 있다. 과거 demo에서 직접 tick하면 실제 시각으로 경로를 정리·선택하고 과거 기상으로 평가할 수 있다. tick 범위는 호출자만이 아니라 전체 사용자다. 실제 삭제·발송·오염은 실행하지 않았다.
- **제안/효과:** test session의 원본 view/revision·주입한 타입·복구 전략을 소유하는 overlay 경계를 둔다. tick에는 명시적 clock·대상·sender 의존성을 주거나 demo와 test tick의 조합을 거부한다. reset에 타입 몇 개 추가하는 것만으로 view 혼합이 해결되지는 않는다.
- **유지/위험/선행:** 실제 평가 엔진을 재사용하는 검증 가치는 유지한다. W11-01과 RF-118의 clock 계약, W10의 cleanup·발송/중복 정책을 먼저 확정한다. 전역 scheduler의 기본 실제 시각을 일괄 demo로 바꾸면 운영 TTL/발송 동작을 훼손할 수 있다.
- **U:** 국내/해외 주입 후 reset, 누락 원본, demo 중 reset, live→demo→test overlay→종료, 과거 demo+두 사용자 경로+mock sender로 원본 hash/cache/clock/정리 대상/발송 수를 확인한다.

#### W11-05 — 스냅샷 이름과 활성 자료 세대의 불변성 (P1 후보, 동시성·덮어쓰기 U)

- **F/원인:** `admin/router.js:107`에서 이름을 정한 뒤 :113의 await capture로 넘어간다. `demo-session.js:31` capture는 start/stop의 exclusive 밖에서 drain한다. `snapshot-store.js:126/:152`는 기존 이름 디렉터리를 교체한다. `data-view.js:87`은 같은 이름이 이미 active면 메타를 다시 읽지 않고 반환하며 :111의 자료 링크는 해당 snapshot 경로를 참조한다. save API에는 active 이름 덮어쓰기 거부가 없다(UI는 demo on 동안 저장 버튼을 숨김).
- **H/영향:** drain 대기 중 두 자동 저장 요청이 같은 snapshot-N을 정하고 뒤 캡처가 앞 결과를 교체할 수 있다. 관리자 API로 active 이름을 재저장하면 링크의 파일 내용과 기존 `.view.json` referenceTime/revision이 서로 다른 세대가 된다; 파일을 직접 읽는 소비자 또는 reload 시 드러날 수 있다. 실제 동시 요청/덮어쓰기는 미실행이다.
- **제안/효과:** 이름을 불변 세대 식별자와 분리하거나 기존/active 이름 저장을 거부하고 이름 할당·게시를 같은 소유 경계에서 보장한다. 준비 검사를 통과한 바로 그 세대를 activate하도록 만든다.
- **유지/위험/선행:** staging/rollback과 active symlink 전환은 유지한다. snapshot 외부 repair 도구(W02)·기존 이름 overwrite 사용 의도를 먼저 확인한다. 참조 ID 변경은 캐시 revision, 복원/도구/목록 호환에 영향을 준다. 현재 exclusive callback은 동기이므로 async finally 일반론으로 추가 버그를 주장하지 않는다.
- **U:** 임시 파일트리와 제어 가능한 drain promise로 동시 두 capture의 고유 결과, active 이름 overwrite 거부 또는 revision 갱신, 같은 이름 재시작/프로세스 재시작, publish 실패 rollback을 검증한다. KIM/KTG index 존재와 payload 완전성은 별도 검사 범위를 정한다.

#### W11-06 — 운영 health의 자료 원본·실행시각·quiet 계약 (P1 계약 정리)

- **F/원인:** `admin/router.js:41`은 active view와 live stats를 결합한다. `data-health.js:98/:103`은 active contentAt보다 live last_success를 우선하고 실제 now로 판정한다. 동일 readDataHealth를 ops collectState도 사용한다. 가시 위성은 catalog:69 NIGHT지만 freshness:23은 sun 미입력 시 quiet를 적용하지 않으며 실제 admin/ops 호출은 sun을 주입하지 않는다. 실행 등록부는 config 기반으로 DISABLE_COLLECTION에 의한 runtime 중지를 별도 status로 나타내지 않는다.
- **H/영향:** 시연 화면 자료는 과거인데 최근 실황 성공으로 ok가 되거나, stats 없는 meta의 복사 mtime이 content 시각처럼 보일 수 있다. 반대로 오래된 demo 내용이 live 장애 판정에 섞일 수 있다. night quiet는 실제 호출에서 작동하지 않는다. 어떤 상태가 운영 경고 대상인지는 정책 결정이 필요하며 실제 오알림은 확인하지 않았다.
- **제안/효과:** `live collection health`와 `active display snapshot`의 source/revision/content time을 명시하고 운영 알림은 의도한 domain만 소비하게 한다. collected/succeeded/content/available/run 시간을 분리하는 기존 필드는 보존한다. quiet에 필요한 관측 지점·일출/일몰 원본 또는 보수적 미판정 정책을 명시한다.
- **유지/위험/선행:** CATALOG의 제품별 기대 주기와 실제 API_OPERATION_REGISTRY는 서로 다른 설명으로 유지한다. cron 문자열만으로 모든 freshness를 만들거나 content가 비었다고 실패로 바꾸지 않는다. RF-100/W03의 false-success·부분 실패와 섞지 말고 원본 계약을 선행 확정한다. 모든 health now를 demo로 바꾸면 실제 장애를 숨긴다.
- **U:** live 최근 성공+과거 demo, live 실패+사용가능 demo, stats 없는 meta 복사, DISABLE on, 일출/일몰 제공/누락·경계시각 fixture를 admin UI와 mock ops 판단 양쪽에서 비교한다.

#### W11-07 — 이벤트 개수의 타입별 의미 (P1, 낙뢰 경로 소스 확인)

- **F/원인:** `admin/data-health.js:30 activeCount`는 eventDriven 자료에서 airports의 키 수를 센다. `data-health-catalog.js:55` 낙뢰가 그 대상이다. `processors/lightning-processor.js:209`는 낙뢰가 0개여도 설정된 모든 공항의 payload를 만든다. 실제 전국 낙뢰 목록은 :253 nationwide.strikes다. `DataGrid.jsx:36`과 `DataCollectionScreen.jsx:81`은 결과를 “N건 발효”로 표시한다.
- **H/영향:** 유효한 무낙뢰 snapshot에도 공항 수가 발효 건수로 표시될 수 있다. 이것은 파일 크기 추정이 아니라 생산 payload→계수→문구 연결의 의미 불일치다.
- **제안/효과:** catalog/adapter에 `count/value/unit/meaning/coverage`를 타입별로 둔다. 낙뢰 strike·영향 공항·경보 발효를 서로 구별해 운영 판단을 보존한다.
- **유지/위험/선행:** `null`은 모름, 유효 `0`은 없음이라는 기존 의도를 유지한다. 전국/공항 중 어떤 수와 시간창을 표시할지 W03/W08 자료 정책과 먼저 맞춘다. 공항별 strike를 합산하면 같은 낙뢰 중복 위험이 있으므로 단순 합산 교체는 금물이다.
- **U:** 무낙뢰·여러 공항에 걸친 1 strike·부분 coverage·자료 없음으로 API 값과 UI 단위를 검증한다. warning/typhoon 등 다른 이벤트의 계수는 타입별 후속 확인 범위다.

#### W11-08 — ‘감시중’과 실제 알림 평가 대상 구별 (P2, W10 경계 반환)

- **F/원인:** `admin/alert-watches.js:10`의 watching은 ETD 시간창만 본다. 목록은 alert_enabled 전체(:30)다. 실제 `alerts/scheduler.js:130`은 사용자당 pickActiveFlight 1건이고 자동 tick은 demo에서 중지(:214)한다. `AlertWatchScreen.jsx:7`은 watching을 “감시중”으로 표시하고 mount/수동 조회만 수행한다.
- **H/영향:** 같은 사용자에 시간창이 겹친 두 경로가 모두 감시중으로 보이지만 실제 하나만 평가되거나, demo로 자동 평가가 멈췄는데도 감시중으로 표시될 수 있다. 목록 목적의 “알림이 안 오는 이유”와 맞지 않는다.
- **제안/효과:** 등록/시간창/실제 선택/평가 일시중지·마지막 평가시각을 구분한다. scheduler의 읽기 가능한 선택 결과를 공통 순수 함수로 공유할 후보이며 admin에서 runTick을 호출하면 안 된다.
- **유지/위험/선행:** 창 밖/종료 경로도 보여주는 운영 의도와 구독 없음 표시를 유지한다. W10의 사용자당1건 및 TTL 규칙을 먼저 확인한다. UI만 감시중을 바꿀지 상태 API를 확장할지는 해당 의미 확정 뒤 결정한다.
- **U:** 같은 사용자 중첩2건/다른 사용자1건, demo pause, 구독0, ETD 경계·수동 갱신 전후를 mock DB/clock으로 비교한다.

#### W11-09 — 백업 완료 표식과 이전 성공본 보존 (P1 후보, 중단 U)

- **F/원인:** `db-backup.js:66` 같은 분 target을 먼저 지우고 VACUUM(:68) 이후 첨부/manifest(:70)를 만든다. 정상 예외는 새 DB/첨부를 정리(:72)한다. listBackups/hasBackupToday(:29/:86)는 DB 파일명/mtime만 보고 manifest를 확인하지 않는다. server-health:53과 `ServerResourceScreen.jsx:133`은 이 최신 파일의 나이로 백업 상태를 표시한다.
- **H/영향:** VACUUM 뒤 프로세스 중단 시 첨부가 없는 DB가 오늘 성공본으로 보이고 시작 백업을 건너뛸 수 있다. 같은 분 재백업 실패는 그 분의 기존 성공본을 먼저 잃는다. 실제 중단/손실은 관측하지 않았다. `db-backup.test.js:56`은 같은 분 재저장 성공을 의도적으로 보호하므로 기존 의도를 무시한 결함 단정은 하지 않는다.
- **제안/효과:** DB+assets+manifest 전체를 완성한 후 완료 세대로 게시하고 list/today/UI가 완료 단위를 사용하게 한다. 마지막 시도 오류와 마지막 성공을 분리하면 오래된 성공본을 유지하며 실패를 알릴 수 있다.
- **유지/위험/선행:** VACUUM의 일관 DB, 해당 DB 참조 첨부, 파일/DB 해시, keep7 동반 삭제, 새 경로만 복원은 유지한다. 파일명 호환·일일 dedup·외부 보관/restore CLI 소비(W09/W02) 계약을 먼저 정한다. publication 변경은 실제 복구 절차 검증 없이는 적용하지 않는다.
- **U:** 임시 DB/첨부에서 VACUUM 후/manifest 전/게시 직전 fault injection, 같은 분 두 번째 실패, 불완전 백업 재시작, keep 정리, 무결성 실패 복원·기존 대상 거부를 검증한다. 실행 중인 운영 DB/backup은 열거나 복원하지 않았다.

#### W11-10 — 자원 지표의 측정 대상과 실패 값 (P2)

- **F/원인:** `admin/metrics.js:10` cpuPct는 loadavg(1분)/논리CPU수 ×100을 100으로 제한한 값이고 `ServerResourceScreen.jsx:68`은 CPU %로 표시한다. :7의 디스크는 `/`의 filesystem이며 data 디렉터리 용량은 다른 함수가 읽는다. statfs 실패는 :12/:18의 0/0으로 반환되고 adminFormat.percent는 0으로 변환한다. instrument:18의 bytes는 server finish의 content-length(없으면0), store-stats는 JSON 직렬화 길이다.
- **H/영향:** CPU 사용률·프로세스 CPU·부하 지수가 같은 측정이라고 오해하거나 용량 조회 불가가 사용량0으로 보일 수 있다. data가 별도 mount인 배치에서 `/` 소진 예측은 data volume을 설명하지 않는다. 실제 mount/부하·성능 수치는 측정하지 않았다.
- **제안/효과:** metric name/unit/source/validity를 DTO로 명시하고 불가 값을 unknown으로 표시한다. 진짜 CPU 사용률이 필요하면 측정식을 바꾸기 전에 운영 질문을 정한다; root와 data volume을 같은 것으로 간주하지 않는다.
- **유지/위험/선행:** 60초/7일 제한·disk cache·간단한 forecast는 유지할 수 있다. 과거 cpu_pct와 새 측정값을 무표식으로 섞으면 추세가 왜곡된다. 지표 명칭/기간/DB 호환을 A14 운영 문서와 먼저 맞춘다.
- **U:** 모의 load/statfs error/별도 volume fixture로 단위·unknown·forecast 대상 확인; content-length 없음의 bytes 표시 확인. 실제 CPU 부하/성능 실행은 별도 승인된 측정 묶음에서만 한다.

#### W11-11 — 관리자 시간대·기간·방문 지표의 표시 계약 (P2, A11 반환)

- **F/원인:** `App.jsx:401` admin은 TimeZoneProvider 없이 AuthProvider만 둔다. `shared/timezone/TimeZoneContext.jsx:3` 기본은 KST라 DataCollection:16/ApiUsage:16의 useTimeZone은 선택 저장값을 받지 못한다. AdminShell:74/Overview:12/ServerResource의 timeLabel/DemoPanel:5는 브라우저 로컬 시각이다. AdminShell의 range 공유(:34)와 Overview:93의 고정 “24시간” 라벨도 다르다.
- **H/영향:** 메인 UTC 선택 후 admin 이동 또는 KST 아닌 OS에서 관리자 내 시각 표시가 달라질 수 있다. Server에서7d 선택 뒤 Overview로 가면 7일 자료를 24시간으로 설명할 수 있다.
- **추가 F/해석 보류:** visits:13은 UTC unique-day, :14는 KST 시간별 **요청 횟수**이고 Users의 두 그래프가 같은 모수는 아니다. visits90일 prune(:18)인데 trends.newVisitorTrend(:34)는 이 테이블로 약6개월 신규방문을 집계한다. 방문 없이90일이 지난 식별자는 이전 달 신규 수에서 빠질 수 있다. heatmap ready는 경과14일이 아니라 방문이 있는 distinct day 14개(:74). UI의 “총/온라인/신규/2주”가 어느 모수를 의도하는지 확인해야 한다.
- **제안/효과:** 선택 표시 TZ와 집계 business day를 구분하고 range·집계단위/보관기간을 응답 및 라벨에 연결한다. 사용자 시간대 provider/formatter 적용은 A11와 공유하되 KST 정기운영 cron을 같이 바꾸지 않는다.
- **유지/위험/선행:** UTC instant 저장과 명시 KST 백업 시각은 유지한다. business-day 또는 방문 모수 변경은 과거 집계 재해석/마이그레이션이 필요하므로 formatter 교체와 분리한다.
- **U:** KST/UTC 선택+비KST OS, 1h→7d→Overview, UTC/KST 자정·주/월 경계, 90일 비방문 식별자/약6개월 trends fixture로 표시와 집계 계약을 확인한다.

#### W11-12 — 예보관 미완성 소비 계약과 상태 전이 (후속 질문, 즉시 결함 판정 보류)

- **F/원인:** `forecaster/router.js:62` claim은 이전 상태/담당자 조건 없이 viewed/자기ID를 쓴다. close(:72)도 같은 공항 권한만 있으면 closed로 갱신한다. detail(:51)은 현재 routes.payload, 목록(:42)은 전체 정렬이다. frontend 소비자 검색은 0건이며 제안서는 프론트 다음 단계임을 명시한다.
- **H/영향:** 두 담당자가 같은 공항 요청을 claim하면 마지막 쓰기가 담당자를 바꾸며 closed를 다시 claim할 수 있다. 이것이 인계/재개 기능인지 충돌인지 계약이 없으므로 구현 결함으로 확정하지 않는다. 현재 payload가 문의 당시 브리핑 재현을 만족하는지도 질문이다.
- **제안/효과:** planned consumer를 만들기 전에 인계/재개·고정본/현재본·페이지 범위를 명시하고 필요한 조건부 update/version을 좁게 선택한다.
- **유지/위험/선행:** 공항별404 격리·DB 공항 목록·router dependency injection·requests-flow 테스트를 유지한다. 담당자 독점으로 갑자기 바꾸면 기존 공동 처리 의도를 깨뜨릴 수 있다. W06/W10의 문의 생성 및 route 수정/삭제 계약이 선행이다.
- **U:** 두 예보관 동시 claim, closed 재claim, 동일공항 인계, 문의 후 경로 수정/삭제의 기대 결과를 먼저 정한 뒤 독립 fixture로 확인한다.

### 7. 문서 대비·유지할 구조·삭제하지 않을 대상

| 계약 입력 | 구현 대조 / 처리 제안 |
| --- | --- |
| Architecture:188~192, operations.md:34~48 | 현재 start/stop은 live 수집 지속·active 포인터·기준시각 결합과 일치한다. admin/router:87/:122/:145의 live 백업/cron 정지/복원 설명은 구형이며 같은 문서로 읽으면 충돌한다. 호출을 바꾸기 전에 주석·오류명·restored 응답의 과거 용어를 현재 계약에 맞출 후보다. |
| snapshot-store의 legacy loadSnapshot:174 | 현재 admin router는 호출하지 않고 snapshot-store.test:54가 실제 소비한다. Architecture도 ‘레거시 복원’을 명시하므로 export 이름만 보고 삭제하지 않는다. 외부 복구 스크립트/운영 절차 소비 여부는 후속 질문이다. |
| entry-sequences.md의 Developer console “test-instance only” | 실제 sidebar는 운영 admin을 의도적으로 허용하고 시연 API도 admin 라우터로 옮겨졌다(scenario:199). 정책은 테스트 trigger/observe와 운영 시연 예외를 구분하도록 좁힐 필요가 있다. 일반 DEV route gate가 전 경로를 보호한다는 해석은 틀리다. |
| DemoModePanel:30 “/admin과 /dev 둘 다” | 직접 import는 TriggerTab만 확인했다. /admin의 7개 screen에는 패널이 없다. 운영 admin이 메인 sidebar의 개발자 콘솔을 통해 시연한다는 경로를 문서에 정확히 적어야 한다; 무조건 /admin에 패널을 추가하자는 요구로 해석하지 않는다. |
| operations.md:40 준비 점검 | 21종/프레임/참조 확인은 존재한다. ‘준비됨’이 모든 후발 자료/격자 의미 검증까지 완료라는 뜻은 아니다. RF-100의 collector previous 분리도 준비 점검으로 증명되지 않는다. |
| organization-lounge.md:25~36 백업·복원 | 정상 예외 시 불완전 새 DB/첨부 제거, 해시 검증 후 새 경로 복원은 일치한다. 프로세스 중단 시 완료 판정(W11-09), restore 중 write 실패의 부분 DB 정리는 문서보다 좁게 추가 검증해야 한다. |
| data-and-time의 UTC instant·선택 표시 TZ·last usable | 실제 시각과 effectiveNow의 목적 분리는 유지한다. 관리자 provider/집계 모수(W11-11), 주입 reset(W11-04), live previous(RF-100)는 서로 다른 소유자이므로 하나의 날짜 유틸로 합치는 것으로 해결되지 않는다. |
| 예보관 구현 제안서:47/:54/:58 | 백엔드·테스트는 있고 프론트는 다음 단계라는 기록과 소비자 검색 결과가 부합한다. router의 “콘솔이 폴링/재현” 주석은 현재 UI가 존재한다는 증명이 아니다. |

보존할 구조는 (1) HTTP 권한 adapter와 DB/파일 서비스 주입 경계, (2) data-view의 단일 활성 원본 및 명시 live 통과, (3) 캡처/준비검사/세션 전환 분리, (4) 수집기/API작업/자료 신선도 세 등록 개념, (5) VACUUM DB가 참조한 immutable 첨부 백업, (6) chartAxis의 순수 계산과 JSX 분리, (7) 마지막 사용가능 상태 보존 및 무자료/유효0의 구별이다. 각 후보는 이 구조를 유지하면서 원본·세대·상태 의미가 흩어진 부분을 명시하는 변경이며 파일 분할 자체를 목표로 하지 않는다.

**W12 반환:** `AdminPage.css:8` 관리자 scope 토큰과 독립 스크롤, :159 native API dialog, :538 custom 예보관 dialog, :617 모바일 규칙을 목록으로 넘긴다. `ApiExecutionDialog.jsx:4`와 `CreateForecasterDialog.jsx:18`의 서로 다른 모달/포커스·Escape/스크롤 구조가 정당한 차이인지, admin 전용 색·크기 토큰이 design token 정책과 맞는지가 좁은 질문이다. 이번에는 CSS 변경이나 브라우저 접근성·반응형 판정을 하지 않았다.

### 8. 검증 근거와 실제 수행 명령

**선행 결과:** coordinator 메시지 `msg_acf41c480e7d`는 admin 30사례 기존 통과를 알려 주었다. `frontend/verification/contracts/admin-console.spec.mjs`의 메뉴/신호/출처·성격/차트축/상태문구/API상세·스크롤/모델정보 등의 기본 계약을 보호하는 결과로만 인용한다. W11-01 환경 mount, W11-04 reset/tick+demo, W11-05 동시 캡처, W11-09 중단 복구를 통과했다는 뜻이 아니다. 이 worker는 해당 실행을 하지 않았다.

**읽은 관련 테스트의 실제 보호 범위:**

| 테스트 소스 | 현재 보호하는 경계 / 확장 필요한 점 |
| --- | --- |
| backend/test/admin.test.js:34 | 직접 router mount한 admin 권한·조회·승인 ID 검증; 실제 server env 조합은 별도 |
| backend/test/demo-session.test.js:44/:59/:68/:122 | start/stop 포인터·capture만 drain·불완전 snapshot 거부·실황 파일 보존; 실제 collector+active cache 전환/동시 overwrite와는 다름 |
| backend/test/snapshot-store.test.js:54/:73/:90/:106/:141/:164 | legacy 복원·준비 blockers·관측 history/immutable 비교 payload; active 이름 재저장·동시 이름은 별도 |
| backend/test/db-backup.test.js:22/:46/:56/:65 | 읽을 수 있는 백업 DB·keep·같은 분 반복·일반 오류; 중단 publication 검증 아님 |
| backend/test/organization-backup.test.js:30/:45 | immutable 첨부+복원·누락/손상 거부; 프로세스 중단·부분 목적 DB fault는 별도 |
| backend/test/requests-flow.test.js:31/:82 | pilot→담당공항 forecaster·claim/detail/close·격리; 다중 담당자 충돌/재개/문의 후 route 변경은 별도 |
| admin-data-health/metrics/visits/trends/alert-watches/process-health/disk-usage 테스트 | 개별 순수 규칙·조회 범위가 있음; active/live 운영 배선과 실패 후 UI 상태는 개별 테스트 성공으로 확장하지 않음 |
| frontend features/admin/lib의 menus/adminFormat/apiOperationSummary/chartAxis 테스트 | 순수 표시 계산을 유지할 근거; API 실패·provider·실제 폴링 수명과 별도 |

**실제로 수행:**

- `git rev-parse HEAD`, `git branch --show-current`, `git status --short`: 기준 HEAD/main 및 기존 미커밋 경로 확인. 완료 직전에도 동일 HEAD/main, AGENTS/CLAUDE/docs/README 수정·docs/refactoring 비추적 상태가 남아 있다.
- `rg --files`, `rg -n`, `nl -ba ... | sed -n ...`, `cat`/`sed`: A09 소스·직접 호출자·정책·기존 보고서·테스트를 읽었다. foreground 애플리케이션/모듈 import 실행은 하지 않았다. 일부 초기 추정 파일명 검색은 ENOENT/일치없음을 반환해 실제 `lightning-processor.js`, `shared/demoMode/useDemoMode.js`로 좁혔다; 이는 앱 검사 실패가 아니다.
- Python 표준 json/path/문자열 처리: 기존 inventory의 records/count를 읽고 A09 원장 대조, W11 본문 UTF-8/LF 및 참조 경로 확인에만 사용했다. 원본 자료 파싱·변환·소스 import나 앱 테스트 실행은 없음.
- `orca-ide orchestration check`를 자연 체크포인트마다 수행하고 heartbeat를 전송했다. 최종 완료는 현재 task/dispatch에 한 번만 보고한다.
- 쓰기 명령은 `artifacts/refactoring/S004/W11.md`의 CP1/CP2/최종 본문 작성·인용 정정뿐이다. tasks/audit/report 등 공통 문서는 주 담당이 반영한다.

**미실행:** 서버·수집·HTTP API 호출·시연/스냅샷 생성·역할/계정 변경·DB backup/restore·외부 알림·test/build/contract/브라우저·성능 측정 모두 실행하지 않았다. 이 문서의 U는 검증 제안이며 실행 실적이 아니다.

최종 문서 대조 결과: A01의 A09 48개와 본 원장 48개가 일치하고 누락·추가·존재하지 않는 원장 파일은 0개였다. 보고서는 UTF-8, CR 없음, 마지막 LF를 확인했다. 이는 문서 형식/목록 대조이며 애플리케이션 검사 결과가 아니다.

### 9. 최종 체크포인트와 미확인 좁은 후속 질문

- CP1(01:32 KST): 권한·콘솔/시연·health·백업 진입 추적을 파일에 보존했다.
- CP2(01:43 KST 기록): 전체48 역할 분류와 위험 경로를 보존했다.
- 최종(2026-09-12 KST): A09 원장48/대표 경로/12개 임시 후보·질문/문서 차이/유지 구조/미실행 범위를 W11에 합쳤다. `tasks.md`의 영역 상태 확정·전역 RF ID 연결은 주 담당 검수로 남긴다.

부분 확인으로 남기는 범위와 재개 질문은 다음과 같다. 다음 배정 시 가장 앞의 권한/스냅샷 세대 질문부터 한 묶음만 좁혀 조사하는 것이 적절하다.

1. **W11-01:** 운영에서 수집을 끄는 공식 설정은 무엇이며, 테스트 mutation은 non-production·별도 DB·별도 명시 flag 중 어떤 조건을 모두 요구해야 하는가? 실제 배포 환경 값은 열람/조작하지 않았다.
2. **W11-05:** 같은 snapshot 이름 덮어쓰기를 사용하는 외부 repair/운영 소비자가 있는가? 이름을 불변으로 만들 수 없다면 active 세대별 디렉터리와 revision을 어디서 소유할 것인가? restart/동시 저장/fault 전체는 미검증이다.
3. **W11-04/RF-118:** 테스트 주입을 demo와 함께 허용할 것인가? 허용한다면 reset 원본, tick 대상 사용자, effectiveNow/실제 TTL, mock sender를 하나의 테스트 session 계약으로 제한할 수 있는가?
4. **W11-06/07/08:** 운영 자료 health는 live 수집 상태만 뜻하는가, 현재 시연 화면 상태도 함께 보여야 하는가? 낙뢰는 전국 strike 수/영향 공항 수 중 무엇을 어떤 시간창으로 표시하며, 감시중은 등록 시간창인가 실제 평가 대상인가? night quiet의 기준 공항·sun 공급자는 누구인가?
5. **W11-09:** DB+assets가 완료됐다는 durable 표식을 어떤 파일/manifest 상태로 정의할 것인가? 같은 분 재백업·프로세스 중단·복원 마지막 DB write 실패에서 이전 성공본과 재시작 재시도를 보장하는가? 실제 운영 백업 무결성·외부 보관·복구 연습은 미확인이다.
6. **W11-10/11:** CPU/디스크 지표는 어떤 운영 판단을 위한 값인가? admin 선택 TZ와 방문 business day·모수·보관기간을 어떤 문구로 보장할 것인가? 실제 volume·긴 시간 데이터·브라우저 TZ/반응형은 미검증이다.
7. **W11-12:** 예보관 claim은 독점/공동 인계 중 무엇인가, closed 재개를 허용하는가? 문의 상세는 문의 당시 고정 경로인가 최신 경로인가? 실제 프론트 구현 시점과 외부 API 소비자는 미확인이다.
8. **경계 반환:** 인증 세션 취소·계정 상태 전이는 W10, 공통 polling과 다중 창 동기화는 W04/A11, collector previous/drain은 W03, CSS·모달 접근성은 W12로 남긴다. 해당 영역의 기존 결과를 A09 대표 흐름 확인만으로 전수 완료 처리하지 않는다.
