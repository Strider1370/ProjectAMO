# RF-002-W08 / A06 모니터링·터미널 진단

- 기준: main HEAD `075eb89a8a2ffe5fde8ccf9bf1cbec954284fa41`, 기존 AGENTS/CLAUDE/docs/README/docs/refactoring 미커밋 보존.
- 허용: 읽기 전용 소스/기존 증거 조사 및 본 W08.md 작성만. 소스·설정·영구 테스트·공통 문서·데이터 수정, 추가 에이전트, 서버·수집·검사·브라우저·성능 실행 없음.
- 입력: AGENTS.md, tasks.md W08 및 배정/반환 계약, Architecture, data-and-time/entry-sequences/map-and-layers/검증 정책, A01 inventory, W04 폴링 계약. W01~03은 관련 계약만 참조.
- 상태: 독립 조사 1묶음 반환 / A06 부분 확인. 전체 파일 분류와 위험별 대표 경로를 확보했으며 실행·장시간 검증과 아래 후속 질문은 남아 있다. 구현 후보는 사용자 미선정.

## 중간 체크포인트 CP1

- MonitoringPage는 legacy 컴포넌트/alert 엔진을 실제로 import하여 운항/지상 화면을 구성한다. legacy 이름·CSS 크기를 삭제 근거로 쓰지 않는다.
- shared useSnapshotPolling의 profile별 차이를 확인했다: monitoring initial은 필수 alert-defaults와 묶이고, changed의 null은 undefined로 바뀐다. W04 계약을 실제 카드/경보 소비자와 대조 중이다.
- Terminal은 production pathname에 진입하며 별도 60초 8요청 묶음과 30초 clock/3초 codeshare/9초 또는 30초 frame timer를 갖는다. feed가 없을 때 2026-08-02 fixture로 돌아가는 경로와 실황 날씨에 fixture 값이 남을 수 있는 경로를 확인 중이다.
- nextDisplayedSimulation은 board frameCursor로만 교체 경계를 판단하지만 weather/rail은 weatherFrameCursor만 전진한다. 화면 전환 후 업데이트 정지 또는 중간 교체 가설을 실제 호출에 연결했다.
- 주 담당 follow-up msg_0ff4834a1a26: W04 defaults/null/body 실패 심화, 기존 browser-main 214pass/58fail/85skip의 관련 오류 대조 요청. 이 수치는 주 담당 실행 결과이며 W08의 실행이 아니다.
- 다음: terminal processor 게시·운항 날짜/예보 source, legacy 경보/rotation cleanup, 기존 browser selector/geometry 증거 → 범위별 후보·유지·미확인 보고.

## 중간 체크포인트 CP2

- 터미널의 KST 표제 시계는 UTC+9 변환, nowKst 예보 선택은 브라우저 로컬 getter라 비-KST OS에서 불일치한다. AMOS 경계는 결측을 null로 게시하지만 terminal finiteNumber는 Number(null)=0으로 읽는다.
- board의 frameCursor가 0이 아닌 상태에서 weather/rail로 전환하면 해당 커서가 멈추므로 nextDisplayedSimulation이 새 편성을 계속 거부하는 정적 경로를 확인했다. 단 실제 장시간 운용·브라우저 재현은 미실행이다.
- 지상 경보 타임아웃은 legacy/App.css:4112의 signage 경보 display:none과 연결된다. 8월 11일 문서는 활성 경보 유지, 8월 12일 문서는 경보 슬롯 제거를 요구하므로 CSS 오타로 단정하지 않는다.
- 기존 ops actual/expected PNG를 파일로 열어 비교했다. expected는 METAR/TAF 무자료 상태, actual은 카드와 TAF·경보가 실제로 표시되는 상태여서 큰 pixel diff를 순수 CSS 회귀로 해석할 수 없다. 신규 캡처·브라우저 실행은 없다.
- 다음 재개: 후보 카드와 전체 A06 66개 파일/자원·타이머/소비 경계 목록, W12 반환 및 좁은 후속 질문 작성.


## 반환 요지와 판정 기준

A06의 `legacy`는 삭제 가능한 옛 앱이 아니라 MonitoringPage가 지금 렌더하는 카드·예보·경보 구현이다. 메인과 공유하는 것은 폴링의 상태 기계, MapView, WeatherIcon/visual resolver와 일부 계산이며, 모니터링의 초기 필수 설정·화면 게이트·경보 평가는 별도 책임이다. 터미널은 실제 운항 API를 쓰는 production 진입점이지만 고정 시뮬레이션을 실패 대체 자료로 계속 사용한다. 그 자체는 주석·테스트로 확인되는 의도이고, 실황 표시·부분 실패·편성 적용 경계가 분리되지 않은 부분이 후보이다.

- **사실(F)**: 현재 기준 소스의 분기·호출·자료 모양 또는 주 담당이 남긴 기존 실행 산출물. 코드로 보이는 오류 조건은 실제 운영 발생 횟수와 구별한다.
- **가설(H)**: 특정 응답 순서·시간대·전환을 주었을 때의 사용자 영향. 소스 추적으로 조건을 좁혔으며 새 실행은 하지 않았다.
- **미검증(U)**: 브라우저 재현, 네트워크 순서 제어, 장시간 메모리/자원 수, 실 upstream 정상/실패 비율, 실제 운영 타임존. 기존 테스트가 있다는 사실을 이번 통과로 쓰지 않는다.
- **범위 기준**: A01 `inventory.json`의 A06 66파일 = JS/JSX 53 + CSS 3 + 자산 10. 전체 tracked 2248, 대표 graph 601 source modules/1297 edges/금지 import 0/SCC 0은 분류 입력일 뿐이며 자산·동적 소비·런타임 안전성·미사용 판정의 증거가 아니다. 파일별 원장과 경계 파일은 아래에 반환한다.
- 읽은 정책: `AGENTS.md`; `docs/refactoring/tasks.md:78` 반환 형식 및 W08 행; `Architecture.md:60` shared polling, `:81` 모니터링, `:82` 터미널, `:85` legacy; `docs/policies/engineering/data-and-time.md`, `docs/policies/engineering/entry-sequences.md`, `docs/policies/engineering/map-and-layers.md`, 관련 검증 계약. W04 입력은 `docs/refactoring/audit/W04.md:95` 초기/optional/changed/복구 메모이고 W01~03의 실행/수집 경계는 계약 입력으로만 썼다.

## 실제 진입·자료·소비 흐름

### 모니터링

1. `frontend/src/app/App.jsx:378`의 `/monitoring` 분기 → 폭 <=719이면 `location.replace('/')`(:383), 그 밖은 AuthProvider → MonitoringPage. 모니터링의 mobile 코드가 남아 있는 사실과 현재 mobile 진입 허용은 다르다. `/monitoring?mode=ground`는 `MonitoringPage.jsx:107`에서 초기 모드를 고르고, 모드 버튼은 같은 컴포넌트 상태와 history를 바꾼다(:520).
2. `MonitoringPage.jsx:255` → `useSnapshotPolling`에 `monitoringApi.js` profile 전달. `loadMonitoringInitialData:69`는 18개 자료 fetch 묶음과 필수 `/api/alert-defaults`를 Promise.all로 기다린다. defaults 성공 후 `MonitoringPage:258`에서 공항과 설정을 정하고 그 설정으로 interval을 연다(:247, 기본 30초). 메인 profile과 interval·초기 의존성이 다르므로 단순 API 이름 통일로 회복성이 같아지지 않는다.
3. `monitoringApi.js:15` 데이터 목록: airports, metar, taf, amos, warning, kmaSpecialWarning, sigmet, airmet, lightning, groundForecast, groundOverview, environment, airportInfo, warningTypes + hsrMeta/hciMeta/satMeta/satVisibleMeta. airports와 warningTypes는 초기 정적 구성이다. 나머지는 hash 또는 graphic key(tm/source hash/updatedAt)로 snapshot-meta diff 후 선택 요청한다(:98,119,147).
4. `backend/server.js:337` sendLatest → `readLatest:284` → active DATA_ROOT의 latest 또는 store cache. 정상 무자료는 **503**이며 `{airports:{}}`/공항별 빈 배열은 유효 JSON이다. 이 API가 정상적으로 200/null을 게시한다고 주장하지 않는다. `/api/alert-defaults:1126`는 정적 shared defaults를 ETag로 반환한다. active root는 `server.js:90`, `/data:210`; demo/live view의 revision은 snapshot-meta(:552)에 포함되지만 monitoring snapshot 비교에 없다(W04 연결).
5. 렌더 소비: METAR/AMOS → MetarCard 또는 GroundCurrentWeatherCard(:666); groundForecast/environment → 지상 현재·예보; TAF → TafTimeline(:689); warning/KMA special → WarningList(:655); airportInfo → AirportInfoDocument를 감싼 slideshow; 그래픽 meta/advisory/lightning → MonitoringMap → 공용 MapView(:81). **dashboard-root 전체는 data.metar truthy 조건(:747)**이다. 따라서 부분 자료별 계약뿐 아니라 루트 가용성 게이트도 중요하다.
6. 경보: `MonitoringPage:306`의 변경 자료 effect → alert-engine → 8개 trigger(low_visibility, high_wind, weather_phenomenon, low_ceiling, taf_adverse_weather, lightning_detected, taf_change, taf_new_period) → alert-state의 공항별 key/cooldown/firstFired → dispatcher callback(:231) → 최대 20 active rows/valid keys → AlertPanel·AlertSound(:728), METAR field/TAF timeline/range ring highlight. WarningList의 실제 공항경보와 이 사용자 임계값 경보는 별도 체계다. ground는 임계값 판정·popup/sound/highlight를 멈추되 cooldown 연속성을 위해 기존 state를 보존한다(:312).
7. Map wrapper는 basemap switch/resize/flyToAirport를 imperative ref로 보내고 8/16/32km ring, 초기 HSR/lightning, 제한된 MET layer 목록을 설정한다(`MonitoringMap:29,35,39,89,103`). wind/flightCategory/typhoon/routeBriefing을 명시적으로 비활성화한다(:104). 오버레이로 가리는 동안 지도는 mounted 상태다. 지도 내부 source/styleRevision/async cleanup 판정은 W05에 귀속하며 이 보고서에서 복제 감사하지 않았다.

### 터미널 및 수집 경계

1. `App.jsx:375`의 `/terminal[/icao]`는 TerminalPage → DestinationWeatherPage의 `App`으로 직접 들어간다. DEV 조건이나 터미널 fixture 전용 라우트가 아니다. `TerminalPage.jsx:5`의 아직 fixture라는 설명은 현재 live adapter와 다르다. 출발 선택은 `terminalAirportSelection.js:1`의 7공항 허용 목록 + catalog 교집합이며, 임의 ICAO와 RKSI 출발을 포함하지 않는 것이 현재 지원 범위다.
2. `DestinationWeatherPage.jsx:385`는 진입 즉시 및 60초마다 `loadTerminalLiveWeatherData:150` 실행. 국내 airports, 해외 navdata catalog, 국내/해외 METAR, AMOS, terminal-flights, ground-forecast, overseas-forecast **8개**를 Promise.all로 읽는다. 각 fetchOptional은 실패를 null로 만든다. 성공한 bundle 전체로 liveWeatherData를 교체한다. shared useSnapshotPolling은 사용하지 않으며 그 차이의 실제 영향은 W08-04에 적었다.
3. 생산: collector-registry `:50` ground_forecast(기본 3시간), `:51` terminal_flights(기본 1분), `:52` overseas_forecast(기본 1시간) → index의 processor registry(:149,152)와 schedule 목록(:238,243,244) → 각 process → store.save → server의 `/api/ground-forecast:906`, `/api/terminal-flights:910`, `/api/overseas-forecast:911`. 수집기 실제 실행·설정 override 값·upstream 응답은 미실행/미확인이다. 1분 수집으로 바뀐 코드에 5분 설명이 남아 있는 `terminal-flight-processor.js:134`는 문서 drift이다.
4. 운항: KAC response HTTP/JSON/resultCode 확인 → 전국 row → 국내 도착시각 lookup + 조건부 IIAC 인천도착 lookup → normalizeDeparture → gate가 있는 동일 목적지/정시/탑승구의 codeshare grouping → 7공항 배열/ISO fetched_at/source 게시(`terminal-flight-processor:181,270,298,328`). **이전 gate 추적은 :307 주석으로 의도적으로 정지**했으며 helper/test는 유지한다. 운항 상태어가 gate 변경 표시를 담당한다.
5. `DestinationWeatherPage:304`는 해당 공항 feed row와 fetched_at의 KST 시각을 simulation builder에 전달한다. row/기준시각이 없으면 **2026-08-02 13:00 KST** 고정 fixture(`terminalFlightSimulation:3`)로 돌아간다. 반대로 **유효 빈 배열 []는 fallback 조건이 아니며 실제 빈 운항을 표현**한다. demo active view의 snapshot 자료도 같은 API에서 오므로 `liveWeatherData`라는 변수 이름이 생산 실황 여부를 보증하지 않는다.
6. 운항은 terminalFlightsFromFeed(:325)에서 scheduled/revised/arrivalKst/forecastAnchorKst/ICAO/flightKey로 변환된다. selectTerminalSourceFlights(:362)는 정시 기준 미래 창과 저빈도 공항 최소 편수를 고른다. destination grouping → 보드 compact frames와 도시 단위 destinationFrames를 별도로 만든다(:392,469). transition helper(:527)는 안정/진입/퇴장/도시변경/편변경을 구별하므로 frame 수나 파일 크기만으로 분리 후보로 보지 않는다.
7. 날씨: destinationIcao 우선 조회(`terminalLiveData:21`) → AMOS 보조+국내/해외 METAR 현재 날씨 → 국내 ground hourly 우선/해외 met.no hourly fallback. **예보 조회는 METAR 유무와 독립**(:82). 실제 도착 시각과 국제선 비행시간표의 `forecastAnchorKst`는 구별하며 추정치를 실제 도착처럼 표시하지 않는 구조를 유지한다. ground-forecast processor는 short/mid/village 소스와 source_status/hourly_status를 보존하고 품질 하락 시 이전 날짜별 usable 값을 보존(:461,484,590); 해외 processor는 UTC 입력을 KST slot으로 정규화(:52,71), 공항별 실패면 `_stale` 이전 예보 유지(:133).
8. Board는 슬롯별 비행편/시각/게이트/예보, weather는 3일 시간대 strip, rail은 24시간 strip+주간+편명 목록을 쓴다. WeeklyWeatherScreen의 넘치는 편명 목록은 4초 rotation/finish timer를 별도로 정리한다(:131). 날씨 화면은 모든 도시를 우선순위·수용량에 맞춰 순환하고 boarding frame과 수가 다를 수 있다. 이 차이는 의도적이지만 갱신 경계가 따라가지 못한다(W08-03).
9. 실황 출처는 footer의 고정 문자열(`terminalShared.jsx:183`), 관측 시각은 CurrentWeatherBlock(:364), 예보 출처는 adapter의 forecastSource/data source 속성에 나뉜다. frontend가 `_stale`와 실패 구분을 일관되게 표시하지 않으므로 서버의 last-good 정책만으로 소비자 의미가 보전되지는 않는다.

## 기능별 자원·이벤트·가시성·cleanup 원장

| 소유자 / 기능 | 생성·상태·가시성 | 갱신·정리 사실 / 남는 질문 |
|---|---|---|
| MonitoringPage 선택·모드·설정 | localStorage 공항/TZ/theme/basemap/traffic/advisory/displayMode, query mode, body class | 공항 변경 저장, body class cleanup; selectMode는 pushState, popstate에 맞춘 복원은 확인되지 않아 back/forward 질문 반환 |
| shared polling profile | initial Promise.all, meta diff, interval, same-window data-view-changed listener | hook mounted guard/interval clear/listener off 있음. 초기와 poll/deferred 응답 세대는 W04 소유. initial defaults 실패 복구와 null 의미는 W08-01 |
| MonitoringPage clocks | 현재 clock 1초(:285); highlight 존재 시 1초(:614) | 각각 clearInterval. AlertPanel 자체 highlight tick도 조건부 1초/cleanup(:35); 중복 tick이 비용 결함이라는 측정은 없음 |
| MonitoringPage viewport | matchMedia change(:196), resize(:205), control menu pointerdown/keydown(:298) | removeEventListener 존재; <=719 initial route redirect와 별도로 resize 때 inline/task state가 남는 과도기 코드 |
| 1920 canvas / DPR | canvasScale은 >=1200에서 width/1920, DPR descriptor를 native×scale로 override(:220) | 레이아웃 effect cleanup이 descriptor 복원. 터미널은 좁은 폭에도 scale을 유지하므로 두 helper가 비슷해도 동등하지 않음 |
| Header/Settings | 공항 dropdown document mousedown; settings 모달/inline local state | Header:12 add/off. Settings:169 ground 탭 제한, 정상적인 로컬 설정 초안; 개별 접근성 전체검증은 W12/주 담당 |
| METAR·ground 현재 | props 기반 렌더, 공용 WeatherIcon/resolver/feels-like/sunrise | 별도 fetch/interval 없음; observation time와 선택 TZ를 전달. METAR root/카드 gate 및 결측 숫자는 별도 후보 |
| TAF 3표현 | legacy TafTimeline, shared weather 계산, route-mode adapter | 카드/표/타임라인이 현재 선택 가능하며 삭제 대상 아님. 자체 timer 없음; parent 1초 rerender와 UTC instant 자료 사용. 각 모드 모든 시간 경계는 미검증 |
| WarningList | airport warning + ground KMA warning; measure refs/pages, 4.2초 교체, 1초 전환 | ResizeObserver disconnect 또는 window resize off, interval/timeout clear. pages 리셋 후 fallback 전체 rows가 있어 빈 pages만으로 경보 소실을 주장하지 않음; signage CSS 숨김은 W12 반환 |
| ground signage 예보 | hourly/weekly 두 패널 mounted, aria-hidden/inert, 12초 한 번 timeout을 activeView 변경마다 재예약 | groundForecastTimer cleanup; **새 예보 데이터가 12초 회전을 재시작하지 않음**. 숨긴 패널도 props 갱신. inert 문자열 React console 경고는 실제 오류 산출물에 존재하며 boolean 계약 검토 필요 |
| ground classic | 두 클래식 예보 패널 동시 표시, 선택값은 localStorage | 정상 사용 경로. signage와 columns/issuedAt표기가 달라 단순 중복 삭제 금지 |
| alert state/dispatcher | 모듈 history key, callback 1개, first/lastFired, previousData, validKeys | callback 등록 해제(:244), 공항별 cooldown 제거 정책 있음. clock-only 진행에서 평가 안 되는 경로 W08-07; A→B→A cooldown/행 복원은 후속 |
| alert sound/preview | AudioContext lazy 생성, severity별 여러 setTimeout, played IDs 정리 | beep oscillator는 정해진 시간 stop; 예약 timeout 취소와 AudioContext.close는 없음(W08-08). page preview의 :395/:408/:439 timeout도 소유 handle 없음 |
| monitoring slideshow | config localStorage, image IndexedDB, schedule 상태 30초, slide dwell timeout(최소5초), 두 화면 범위 중 선택된 하나 | useMonitoringSlideshow:20/50 clear; object URL replace/unmount revoke(:55,73); IDB 성공 close(:249,261,273). 로컬 시간 schedule :156, midnight wrap 지원. 다중 파일 저장 응답 역전/실패 close는 후속 |
| slide overlay / 문서 fit | leaving+shown 2개 레이어, duration timeout; 문서 fonts.ready와 ResizeObserver | overlay:30 timeout clear; WxInfoSlide:19 canceled flag/observer disconnect(:62). 동일 slide ID content ref와 duration 변경 중 전환 정리는 좁은 후속 질문 |
| MonitoringMap | ref로 basemap/fly/resize, styleReady boolean, map-panel overlay | map은 슬라이드 뒤에서 계속 mounted; 스타일 교체 가시성/자원은 공용 MapView 소유(W05). 부모가 mapStyleReady를 false로 리셋하지 않는 의미는 로딩 UX 후속 |
| Terminal polling | 60초 8요청 bundle, 별도 liveWeatherData | mounted bool+clearInterval, HTTP abort/in-flight/generation 없음(W08-04); Catalog도 매회 요청하지만 비용 측정 없이 낭비라고 단정하지 않음 |
| Terminal clocks/codeshare | now30초(:401), codeshareTurn3초(:410), resize(:419) | interval clear/resize off. codeshare timer는 board 셀 회전; rail FlightRow는 편명을 쌓거나 별도 행으로 펼치므로 차이 유지 |
| Terminal screen/frame | board9초, weather/rail30초 autoplay interval(:444), 전환 finish 1800/1250ms timeout(:430) | replay callback 변화가 interval 재예약하므로 실효주기를 단순9/30초로 측정한 것은 아님. timeout clear on view button/airport/unmount; keyboard1/2/3는 직접setView하여 같은 cleanup 경로를 쓰지 않음 |
| Terminal replay control | R key, 모션 설정의 requestAnimationFrame(:464/470/476), view/path selection | keydown off; motion RAF cancel은 없음. 빠른 view 변경 중 이전 closure의 커서 전진은 좁은 재현 질문 |
| Terminal rail list/settings | RollingFlightList interval+finishTimer; settings document keydown/pointerdown | 목록 overflow/row signature 변경과 unmount 때 양쪽 clear(:155). 설정 이벤트 off(:152). bounded frames/slot identity 유지 |
| Backend collector 자원 | 수집별 scheduler/lock, signal-aware KAC/IIAC request, 해외 순차 API | 공통 실행·저장/abort는 W03 계약. 해외 루프 abort 후 unvisited 이전 공항 보존 여부, IIAC 실패/skip 의미는 아래 경계 반환. 직접 수집하지 않음 |

## 후보 카드

### W08-01 — 모니터링 최초 자료와 필수 설정의 실패가 화면·자동 복구를 함께 막음 (W04-03 등 연결)

- **F / 원인·경로**: `monitoringApi.js:3` fetchJson의 try 안 `return res.json()`(:7)은 await하지 않아서 body parse 비동기 rejection이 해당 catch로 들어가지 않는다. HTTP optional 실패는 null/undefined로 정리하지만 잘못된 JSON 1개는 loadEntries Promise.all(:36)과 initial(:69)을 실패시킨다. 필수 alert-defaults 실패도 동일하다. `MonitoringPage:247` interval은 alertDefaults가 있어야 숫자이며 initial 실패 시 :258 onInitial이 실행되지 않아 null이 남는다. shared hook의 initialError(:40)와 loading=false는 표시되지만 재시도 버튼이 없다(:741).
- **F / null 소비 세분화**: changed `value ?? undefined`(`monitoringApi:66`)는 명시적200/null을 실패 보존으로 바꿔 shared merger의 null=clear 계약과 다르다. 불완전 변경이면 snapshot advance를 하지 않아 재요청된다. 정상 sendLatest 무자료는503이므로 200/null은 경계가설이며, 유효 `{airports:{}}`는 통과한다. 초기 METAR503만 있고 defaults 등은 정상인 경우 error가 아니라 data.metar=null이 되어 :747 루트가 비고 나머지 정상 자료도 보이지 않는다(다음 정상 poll에서 회복 가능).
- **H / 영향**: 첫 defaults 실패 또는 optional malformed JSON으로 장시간 벽면 화면이 운영자 reload 없이 회복하지 않을 수 있다. API 일시실패의 범위를 한 데이터셋으로 제한하지 못한다. 이후 수동 initial 성공에도 hook이 initialError를 지우지 않아 오류 overlay가 남는 문제는 W04에 연결한다.
- **제안·효과**: shared hook의 초기 retry/error reset 계약을 먼저 결정하고, monitoring의 필수 설정/자료별 optional 결정을 그 계약으로 표현한다. decode 실패를 같은 실패 상태로 수렴하고 루트가 요구하는 최소 자료/부분 표시 조건을 명시하면 회복과 정상 빈 자료 처리가 리뷰 가능해진다.
- **위험·선행**: 설정 없이 alert를 평가하면 임계값이 달라질 수 있어 임의 기본설정 삽입은 금지. 필수 UI와 경보 시작 gate를 분리할지 A02와 결정. null을 모두 지우거나 모든 데이터셋을 한 번에 재요청하는 변경은 last-good/부하 계약을 바꾼다.
- **U / 필요 검증**: existing monitoringApi.test/useWeatherPolling tests와 주 담당 제어 fixture로 (a) 최초defaults503→200 (b) optional200 malformed→valid (c) METAR503만 (d) changed503/200null/200empty object를 각각 관찰. 초기/복구 overlay, 건강한 카드, timer 수, snapshot commit, 후속poll 횟수를 확인. 이번 실행 없음.

### W08-02 — production 터미널에서 fixture·실황·결측의 출처가 합쳐짐

- **F / 원인·경로**: `App.jsx:375` → `DestinationWeatherPage:307`의 의도적 fixture fallback; `terminalFlightSimulation.js:300` known destination이 고정 current/forecast를 가져오고 live row로 운항정보만 덮는다. `terminalLiveData:93` 관측 없으면 withForecast 반환, :112 field별 기존 current 유지; 전체 fixture와 실황 위 fixture 날씨 잔존은 서로 다른 경우다. footer `terminalShared:187`은 항상 “한국공항공사 실시간 운항정보”이다. `terminalLiveData.test.js:90` 및 `terminalFlightSimulation.test.js:263`에 fixture 보존 기대가 있어 실수로 추가된 코드라고 주장하지 않는다.
- **H / 영향**: 최초 실패/재실패/미지원 목적지/active demo view에서 고정 운항 또는 일부 고정 날씨가 현재 실황 문맥으로 읽힐 수 있다. source/time/last-good를 상위 모델에 싣지 않아 어떤 값이 보존된 관측인지 예시인지 소비자가 구별하기 어렵다.
- **제안·효과**: simulation builder와 frame renderer는 유지하면서 입력을 `fixture / usable snapshot / partial stale / unavailable`로 구분하고 source·reference instant를 전달하는 경계 후보. 운영 화면 실패 정책과 검증용 고정 fixture의 접근 방법을 정하면 fallback을 감춘 채 확대하지 않을 수 있다.
- **위험·선행**: 실제 사용자 요구가 “항상 채워진 시연 화면”인지 “실황 운항 게시판”인지 최종 확정 필요; historical fixture를 무조건 제거하면 저빈도 공항의 검증·presentation 계약이 깨진다. W03 active data view와 A11 source/freshness 계약 선행.
- **U / 필요 검증**: 첫 flights503, 정상운항→METAR-only503, 정상→flights503, unknown ICAO, 유효empty[]를 따로 주고 보드/날씨/rail의 값·출처·시간 표시 확인. dated fixture 7공항 보존과 live no-data 표시를 별개 계약으로 확인. 실제운영 영향 미측정.

### W08-03 — 새 편성 적용 조건이 board cursor에 묶여 다른 화면에서 멈춤

- **F / 원인·경로**: `DestinationWeatherPage:317` nextDisplayedSimulation(current,next,frameCursor) 호출은 board cursor만 전달한다. helper `terminalFlightSimulation:520`은 current.frameCount로 나눈 나머지가0일 때만 next를 받는다. weather/rail replay는 `DestinationWeatherPage:431`에서 **weatherFrameCursor만 증가**한다. destinationFlights(:343)는 현재 simulation의 row를 mergeTerminalLiveWeather에 전달하며 이 함수(:77)는 flights feed를 입력받지 않아 지연/게이트를 새 feed row로 즉시 overlay하지 않는다. “도시순서는 유지, 편상태 즉시갱신”이라는 :442 주석은 이 소비 경로와 맞지 않는다.
- **H / 좁은 사례**: current.frameCount=4, board cursor=1에서 rail로 전환→그 뒤 새 feed가 와도 helper는 계속 current를 반환. 반대로 board cursor=0인 채 weather부터 시작하면 매 feed마다 도시 순서가 순환 중에도 교체 가능하다. 한 frame인 경우 modulo=0이라 이 정지 조건이 발생하지 않는다.
- **제안·효과**: 활성 view의 queue generation/cycle boundary를 명시하거나 편성순서 snapshot과 최신 row 상태를 별도로 적용. 순서 안정성을 유지하면서 flightKey별 gate/delay를 새 feed에서 읽는 책임을 분리하면 자료가 조용히 동결되는 조건을 없앨 수 있다.
- **위험·선행**: 목적지 capacity와 frame 수, 신규/삭제/공동운항 flightKey가 바뀌는 경우 현재/퇴장 슬롯 대응이 깨질 수 있다. 보드와 weather/rail의 의도적인 서로 다른 queue를 하나로 합치지 않는다. W08-02의 source model과 업데이트 허용 시점 선행.
- **U / 필요 검증**: pure helper test(:309~328)는 modulo만 확인하므로 부모 integration에 4frame, cursor1, view2/3, 3회 feed 갱신, gate/delay변경을 넣어 queue·row 표시를 확인해야 한다. board→rail→board와 키보드/버튼 전환, frame 수 축소·autoplay0도 포함. 신규실행 없음.

### W08-04 — 터미널 자체 polling은 요청 세대와 부분 실패의 last-good 경계가 없음

- **F / 원인·경로**: `DestinationWeatherPage:387` refresh는 mounted bool만 보고 결과 bundle을 전부 set한다. :393 setInterval은 완료를 기다리지 않는다. `terminalLiveData:132` optional fetch는 HTTP/body 실패→null, :150 Promise.all은 자료별 성공/실패 상태와 기존 snapshot을 합치지 않는다. 따라서 60초 이상 지연된 A와 뒤 요청 B가 겹칠 수 있고 B 후 A가 끝나면 A가 마지막 state가 된다. unmount 후 setState는 막지만 네트워크 자체는 계속한다.
- **H / 영향**: 건강한 feed를 받은 뒤 부분 실패로 liveWeatherData의 해당 키가 null이 되면 이전 실제 관측을 보존하지 않고 W08-02의 fixture/current fallback으로 돌아갈 수 있다. 장시간 느린 endpoint 하나가 전체 bundle 교체를 지연한다. 실제 네트워크 지연·재현 빈도·메모리 증가는 미측정이다.
- **제안·효과**: shared polling에 억지로 모든 endpoint를 넣기 전 terminal의 정적 catalog/실황 데이터·실패 sentinel·snapshot generation을 정의한다. 단일 in-flight 또는 최신 세대 commit+Abort 정책과 필드별 last-good/validempty 구분을 도입할 후보. W04 공통 hook의 race를 그대로 상속하면 해결이 아니므로 선행 수정과 함께 검토한다.
- **위험·선행**: 실패를 오래 보존하면서 source stale를 숨기면 정보 신선도를 더 나쁘게 만들 수 있다. validempty flights[]는 그대로 반영하고 partial failure와 분리한다. 서버 ETag는 전송량 절감이지 응답 순서/React state generation 보증이 아니다.
- **U / 필요 검증**: A를90초/B를10초로 응답, METAR만503, flights validempty, unmount 중응답, endpoint 하나 malformed를 제어. 최종 data의 버전, 보존값, abort/in-flight 수, frame update를 확인. 실제 실행 없음.

### W08-05 — AMOS의 정상 결측 null을 terminal 숫자 변환이 0으로 채움

- **F / 원인·경로**: `backend/src/processors/amos-processor.js:118` temperature/dewpoint/humidity는 결측이면 명시적 null이다. `terminalLiveData.js:68` finiteNumber는 Number(value)를 먼저 실행하여 null/빈문자열을0으로 만든다. :97/:98/:100의 AMOS 우선 nullish fallback은 이0에서 멈춰 유효 METAR 값을 쓰지 않는다. departure airportTemperature(:242)도 같은 helper를 쓰고 CurrentWeatherBlock(:365) 온도차 문구까지 영향을 받는다.
- **H / 영향**: AMOS null + METAR 25°C 입력이면 0°C를 현재 기온으로 채울 수 있고 이슬점/습도/체감·출발지 대비 차이도 왜곡된다. 결측 발생이 실제 어느 공항에서 얼마나 있는지는 조사하지 않았다.
- **제안·효과**: source-boundary 숫자 해석을 명시하여 null/undefined/빈문자열과 진짜0을 구별한다. 기존 weather 계산을 유지하면서 결측 fallback의 의미를 회복하는 좁은 후보다.
- **위험·선행**: 숫자 문자열을 upstream 허용 형식으로 볼지 정해야 하고 정상0°C/0%를 결측으로 바꾸면 안 된다. W03 parser 정책은 계약 입력, 이 소비자 숫자함수 수정범위는 작다.
- **U / 필요 검증**: terminalLiveData.test에 null/undefined/''/'0'/0/유효METAR 조합을 출발·목적지와 humidity까지 확인하는 경계 사례가 필요하다. 기존 테스트 실행 없음.

### W08-06 — 터미널 예보 기준 nowKst가 OS 로컬 시간에 의존하고 운항 시간에서 날짜가 사라짐

- **F / 확정 연결**: `DestinationWeatherPage.jsx:360` nowKst는 now.getFullYear/getMonth/getDate/getHours를 사용한다. 반면 `terminalShared.jsx:74` formatKoreanClock와 `terminalLiveData.js:140` kstClockFromIso, overseas processor(:52)는 epoch+UTC9/UTC getter를 쓴다. terminalForecastStrip(:100,144)와 terminalWeeklyForecast(:28)는 nowKst의 date/hour로 표시 칸을 고른다. 따라서 비-KST OS에서 상단 KST clock과 다른 예보 칸 선택이 가능한 코드다.
- **F / 인접 의도·한계**: arrivalKst/forecastAnchorKst는 HH:mm이며 `terminalLiveData:210`은 날짜가 없음을 명시하고 앞24칸으로 제한한다. 해외 hourly는 KST로 정규화하고 daily도 현재는 KST 오전/오후를 쓴다(`overseas-forecast-processor:126`는 큰 시차 목적지 확장 시 offsetMinutes 도입을 명시). 지금 이 차이를 임의 현지변환으로 “교정”하면 이중변환된다.
- **H / 추가 경계**: `terminalFlightSimulation:368` 미래 후보는 scheduled departure만 비교한다. 정시13:00/지연14:00인 아직 출발 전 편도 reference13:10이면 제외될 수 있다. processor의 estimated>scheduled 문자열 비교(:208), 이전 IIAC flight-only lookup은 자정/운항 날짜에 취약할 수 있으나 실제 upstream 사례·보드 제외 정책은 미확인이다.
- **제안·효과**: 우선 nowKst를 명시된 KST instant formatter로 일원화하고, 날짜가 있는 운항 identity/예보 anchor로 확장할 필요를 별도 좁은 질문으로 처리한다. selected display TZ를 쓰는 monitoring과 고정 KST terminal의 차이는 유지한다.
- **위험·선행**: 운항 종료/취소/지연 상태별 표시 정책, fetched_at과 운항일의 의미, 출발일·도착일이 다른 국제선 범위가 선행이다. 단순 HH:mm 정렬 변경으로는 충분하지 않다.
- **U / 필요 검증**: 동일 epoch로 Asia/Seoul·UTC·America/Los_Angeles, KST23:50→00:10의 forecast/date/header 비교. scheduled 지난 지연편, 익일00:xx 도착, 해외3일hourly, 국내정시·저빈도 첫편을 별도로 검증. 테스트 fixture도 OS local Date getters를 쓰는 기존 terminal-signage:235를 함께 검토해야 한다.

### W08-07 — 모니터링 경보 평가가 변경 자료에만 묶여 시간만 흐르는 상태를 놓침

- **F / 원인·경로**: `MonitoringPage:306` effect deps는 data/selectedAirport/alertDefaults/dashboardMode(:355)이고 1초 clock state를 포함하지 않는다. `useWeatherPolling.js:62` 변경 hash가 없으면 setData 없이 돌아온다. 그런데 `alert-triggers.js:121` TAF lookahead와 :265/:325 변화 위험은 now를 읽고, `alert-dispatcher.js:65` quiet hours와 alert-state cooldown(:47)도 시간을 읽는다.
- **H / 영향**: 동일 snapshot이 장시간 지속되면 TAF 위험이 lookahead 창에 진입하거나 quiet hours가 끝나도 다음 다른 자료 변경까지 판단/valid keys가 유지될 수 있다. 다양한 graphic meta 중 하나라도 바뀌면 전체 data 객체가 새로 되어 우연히 평가되므로 “항상 멈춘다”는 주장이 아니다. quiet hours/슬라이드 schedule의 기준이 OS local인지 선택 displayTZ인지도 현재 별도 계약이다.
- **제안·효과**: 변경 이벤트와 시간 경계 평가를 구별하고 평가 기준 now를 주입할 후보. interval을 무턱대고1초로 늘리기보다 minute/다음 경계 등 허용 정밀도를 정하면 중복알림 없이 장시간 상태를 설명할 수 있다.
- **위험·선행**: TAF change/new-period의 previous vs null 비교, 중단 뒤 “지금 위험”만 발화, firstFired/cooldown을 보존해야 한다. 단순 deps 추가는 같은 알림의 재발화·sound 반복을 늘릴 수 있다.
- **U / 필요 검증**: 모든 hash 고정, quiet 종료 경계, TAF slot lookahead 진입/지난시간 제거, ground↔ops 복귀, A→B→A 공항 전환 후 cooldown 기간의 행과 sound를 제어 clock으로 관찰. AlertPanel 시각은 `toLocaleTimeString`(:13), TAF 메시지는 formatUtc 기본값(:155)라 선택TZ 일관성도 같은 후속 묶음에서 확인한다.

### W08-08 — 경보 sound와 예시 timeout은 전환·해제 수명에 연결되지 않음

- **F / 원인·경로**: `AlertSound.jsx:18` AudioContext 생성, :61 반복 beep timeout을 저장하지 않고 effect 반환 cleanup도 없다. oscillator 자체는 :41 stop되므로 끝없이 울리는 oscillator라고 주장하지 않는다. `MonitoringPage:728` ground 전환은 AlertSound를 unmount하지만 이미 예약된 콜백은 살아 있다. page-level preview :395/:408/:439도 동일하게 미등록 timeout이다.
- **H / 영향**: critical 3회 중 첫 beep 직후 ground 전환/소리 끄기/공항 변경을 하면 나머지 beep가 이전 alert·volume으로 울릴 수 있다. context close 누락은 반복 mount 시 자원 수를 늘릴 가능성이 있으나 leak/CPU 수치는 미측정이다.
- **제안·효과**: timeout/context/활성 oscillator의 소유권을 sound lifecycle에 모으고 설정변경·ground·unmount 때 취소할 후보. alerts의 played ID/firstFired 정책은 보존한다.
- **위험·선행**: 매 alerts 배열 변경마다 무조건 cleanup하면 이미 played 표시한 beep의 나머지가 사라질 수 있어 “새 알림 1회” 단위와 batch 수명을 정해야 한다. 수동 음량 예시는 ground에서도 허용한다는 설계 예외(`2026-07-27-monitoring-alert-redesign-design.md:574`)와 자동 경보를 구별한다.
- **U / 필요 검증**: mock AudioContext/timer로 beep0 직후 mode/setting/unmount, rapidpreview, 동시2alerts, remount 횟수를 확인. 실제 오디오 재생·브라우저장시간측정 없음.

### W08-09 — 구독/호환 필드의 실제 소비자가 사라진 범위만 축소 검토

- **F / 원인·경로**: `MonitoringPage.jsx:659` groundOverviewData를 WarningList에 넘기지만 현재 `WarningList.jsx:29` props/함수 본문은 이를 읽지 않는다. monitoringApi의 초기 요청/변경hash/changed snapshot에는 groundOverview가 계속 포함된다. `frontend/verification/monitoring-fixture.mjs:306`의 “WarningList가 groundOverviewData를 읽는다” 주석도 현재와 다르다. 이는 dataset 전체가 불필요하다는 증거가 아니라 **모니터링 소비 경로 한정**이다.
- **제안·효과**: API profile의 요청 데이터와 실제 카드 소비 목록을 맞추면 불필요한 frontend 요청·state update 경로를 줄이고 poll/alert 평가 의미를 명확하게 할 수 있다. 같은 작업에서 큰 legacy 폴더를 일괄 삭제하지 않는다.
- **위험·선행**: main/다른화면 groundOverview 사용은 소유영역 밖이므로 backend endpoint/processor 제거를 제안하지 않는다. ground prose를 되살릴 운영 요구가 있는지 관련 설계 확인 필요. traffic/advisory 저장 설정이 공용 MapView에서 재독해되는 방식도 별도 좁은 질문이다.
- **U / 필요 검증**: monitoring initial/diff 요청 목록과 warning/KMA/empty branch 계약, ground↔ops 전환에서 요청 제거 영향 확인. 불필요 bytes나 시간 절감 수치 측정 없음.

## W12 반환: 실제 CSS·자산 소비와 기존 브라우저 계약 분류

주 담당 `msg_0ff4834a1a26`의 browser-main 종료 요약은 **214 pass / 58 fail / 85 skip**이며 W08의 실행이 아니다. 실제 경로는 `artifacts/refactoring/S004/browser-main-results/`이다. 관련 error-context/console을 읽고 ops desktop actual/expected 두 PNG만 로컬 파일 뷰어로 비교했다. 저장된 “fix snippet 작성” 등 error-context 설명은 산출물 내용이며 이번 작업 지시로 실행하지 않았다.

| 임시 ID / 기존 결과 | 소스·증거 연결 | 반환 판정·효과·위험·선행/필요 검증 |
|---|---|---|
| W08-C01 monitoring visual mobile 4실패 | `App.jsx:383` <=719 redirect; `.dashboard-root` timeout. `monitoring-visual-*mobile/error-context.md` | 정책상 mobile redirect와 visual matrix 불일치. 모바일 화면을 다시 만드는 근거가 아님. W01과 기대 route/selector 정렬, main redirect를 별도 확인 |
| W08-C02 ops/ground/settings/map-panel desktop·ipad visual 8실패 | ops diff .36/.37, ground .25/.25, settings .70/.62, map-panel .40/.38(기존 error-context). `monitoring-visual-monitoring-visual-monitoring-visual-ops-desktop/monitoring-ops-{actual,expected}.png` | actual은 METAR/TAF·경보가 표시되고 expected는 “No METAR/TAF data”다. fixture schema가 옛날 실제 API와 달랐다는 `2026-07-27-monitoring-alert-redesign-design.md:37`와 맞는다. 실제 기능/fixture/레이아웃/지도 로딩이 모두 섞여 있어 전체 diff를 CSS 실패로 합산 금지. W01 fixture deterministic clock/data와 유효한 baseline을 먼저 확정하고 W12가 나머지 겹침/clip 검증 |
| W08-C03 ground signage 활성경보 selector 2timeout | `legacy/App.css:4112` signage `.left-panel-body > .warning-banner {display:none}`; component `WarningList:210`/parent `MonitoringPage:807`는 경보 렌더. 실패 suffix `--23e48-o-a-six-column-weekly-table-desktop`, `--bd8ec-s-six-labelled-metric-icons-desktop` | 숨김이 접근성 tree에서 경보 region 제거를 설명한다. 8/11 layout-polish 문서:70은 활성 경보 유지, **8/12 readability 문서:9,31은 경보 슬롯 제거**를 요구한다. 최신 의도일 수 있어 CSS 오타 단정 금지. A15와 현행 경보 표시 계약 선택 후 W12 layout/테스트 동시 정렬; 단순 baseline 갱신은 정보 제거를 승인하는 효과가 있어 우선 의도 확정 |
| W08-C04 ground geometry 3실패 | suffix `--467fe--sizing-the-three-left-rows-desktop` 16px; `--3a0b2-es-empty-and-partial-frames-desktop`, `--abc3f--removes-the-no-alert-panel-desktop` 23px. spec `monitoring-ground-signage.spec.mjs:11` PRE_CHANGE_CHROME 고정 header/map 좌표, source canvasScale + row 재정의 `legacy/App.css:4111` | 수치 차이는 실제 기존 측정 사실이나 현재 요구 위반인지는 미확인. 경보 슬롯 제거·header/canvas 변경과 같은 의도된 layout 변경을 포함한다. W12에 고정1920/반응형1200경계·classic/signage·경보유무별 겹침/가독성 측정 반환; source 크기 근거 아님 |
| W08-C05 ground reduced-motion 1실패 | expected `0s`, actual `1e-05s`; `legacy/App.css:4148` 0s보다 공용 `frontend/src/app/App.css:284` 0.01ms!important가 우선한다. 공용 CSS :278 주석 “개별 특이도로 이긴다”는 important 차이를 반영하지 못함 | duration은 사실상0에 가까워 motion 접근성 실패로 단정하지 않는다. 명시0과 전역 안전망의 cascade/문서·테스트 정확문자열을 W12/W01에 반환. 전역중복 제거 시 다른화면 motion 영향을 검증해야 함 |
| W08-C06 terminal spacing 1실패 | `terminal-signage.spec.mjs:320` metric/detail/weather/localClock를 무조건 측정. `terminalShared.jsx:370` terminal-weather의 KST 목적지는 local clock을 생략(:380). 실패 snapshot에 제주가 표시되고 :327은 null bottom으로 vertical 호출 가능. metric DOM 자체는 :398, rail은 `WeeklyWeatherScreen:274` ww-middle에 현재 유지 | “.ww-middle 선택자 전부 옛것”이라는 최초 추정보다 **국내 목적지에서 의도적으로 없는 local clock**을 직접 연결하는 근거가 강하다. 해당 screenshot spacing이 기준 미달인지 이 실패만으로는 알 수 없음. 국내/해외 분기 기대요소와 각 간격을 따로 확인 |
| W08-C07 ground inert React warning | ground 실패 console.json에 boolean inert 문자열 경고; `GroundForecastViewport.jsx:19` 두 panel hidden/inert 속성 | 실제 console 사실, hidden panel 차단 기능 실패까지 입증하지 않음. W12가 boolean 형태/aria-hidden/focus/tab 이동과 reduced-motion rotation을 함께 점검 |
| W08-C08 자산 사용 차이 | terminalShared:4~10에서 amo-wordmark/QR/forecast4 이미지 import. `terminalFlightSimulation:38`은 `/Symbols/airlines/*` IATA/custom-symbol 매핑을 사용. A06 `board-af.png`, `board-jal.png`, `board-sq.png`, `board-header-plane.png`는 현재 frontend/src 문자열 검색에서 소비자가 발견되지 않음 | A12 자산전용 전수 참조/동적경로·과거 문서·배포목록 확인 후보로만 반환; 이번에 삭제하지 않음. 항공사 IATA와 traffic ICAO 로고 체계의 의도적인 차이·fallback을 유지. 용량 자체는 미사용 증거가 아님 |

ground console의 401은 endpoint URL 없는 기록이어서 특정 API 실패로 단정하지 않았다. Mapbox net::ERR_FAILED는 fixture의 `monitoring-fixture.mjs:368` 외부 Mapbox abort와 연결되므로 생산 지도장애 증거로 쓰지 않는다. GPU stall 메시지 역시 성능 측정이나 leak 근거가 아니다.

## 유지할 구조·확장점·의도적 과도기

- `useSnapshotPolling`의 producer(profile)/merge/consumer 분리와 undefined=보존/null=명시empty 계약은 유지할 가치가 있다. monitoring의 초기 defaults와 terminal의 usable snapshot 정책을 정확히 표현하는 것이 선행이며 일괄 통합이 목표가 아니다.
- MonitoringMap이 메인 MapView를 재사용하되 소규모 지원 layer/profile을 전달하는 경계, 공용 WeatherIcon/registry/resolver/기상 계산은 유지한다. legacy 접두어가 중복 구현 증거는 아니다. MapView 크기는 후보가 아니다.
- legacy alert의 이전/현재 비교, 재개 시 null previous, 공항별 cooldown, firstFired 기반 blink 및 실제 공항경보와 임계 경보의 구분을 보존한다. 새 time tick은 이 동작을 검증한 뒤 도입해야 한다.
- ground classic과 signage는 설정으로 현재 선택되는 두 표시 계약이다. 12초 회전이 새자료 도착으로 초기화되지 않는 timer 분리, hidden layer 두 개의 측정/전환 구조는 의도적이다. 오래된 phone task 코드도 route guard와 별개로 남겨둔 과도기이며 삭제 범위 승인 없이 정리하지 않는다.
- slideshow는 config migration/validation, availability에 따라 빈 wxinfo/image slide 제외, 최소 dwell, 자정 걸친 daily schedule, IndexedDB image와 objectURL lifetime, 2-layer transition/문서fit를 각각 나눈 구조가 있다. 라이브 지도 보존과 hidden overlay resource 정책을 먼저 정의한다.
- 터미널의 source-row adapter / queue builder / transition helper / 각 screen renderer 분리는 유효하다. 저빈도 공항 dated evidence와 fixture, 미래 첫편 창, 같은 공항에서 여러 도시를 빠짐없이 순환시키는 capacity·codeshare 규칙은 보존해야 한다.
- `forecastAnchorKst`는 국제선 추정 예보 기준이고 실제 arrivalKst와 구별한다. KMA probability와 met.no precipitation amount 단위 차이, 해외 hourly KST 정규화와 향후 daily offset 확장점은 의도적이다. source/status/date를 잃지 않는 확장이 필요하다.
- terminalFlightProcessor previousGateLookup 비활성은 :307에 이유가 있다. 이를 “미사용 helper 발견”만으로 삭제하거나 다시 활성화하지 않는다. KAC/IIAC/AMOS/ground/overseas의 시간·실패 정책을 모든 API에 동일하다고 가정하지 않는다.

## 소유 경계로 돌려보낼 좁은 생산자 질문

- **W03 연결 B1**: `terminal-flight-processor.js:158` IIAC 실패는 empty Map, 스케줄 skip은 null이다. :311의 `incheonArrivals || previousIncheonArrivals`는 실패 empty Map일 때 이전 lookup을 쓰지 않고 KAC fallback으로 떨어진다. 이전에 알던 인천 도착을 API 실패 때도 보존해야 하는가? 실패와 유효무자료 구분·당일 flight identity가 선행이다. 기대효과는 부분 실패 범위 제한, 위험은 전일 도착시각 오용, 필요검증은 skip/503/유효empty/날짜변경 조합. 전체 producer 변경 제안으로 확대하지 않는다.
- **W03 연결 B2**: overseas-forecast-processor:122는 abort 시 loop break 후 :140 현재 result를 save한다. 아직 순회하지 않은 공항들의 previous usable 데이터가 결과에서 빠지는지, dispatcher가 이 반환을 별도로 막는지 W03의 공통 abort/save 계약으로 확인해야 한다. terminal consumer는 stale를 알지 못하는 점과 연결된다. 직접 abort/수집 실행 없음.
- **A11 연결 B3**: KAC `fetched_at`는 수집 instant이고 개별 운항은 HH:mm이다. 과거 snapshot 재생의 기준시각·운항일·OS TZ·source label을 어떤 화면 모델로 전달할지 확인. demo root pointer의 revision을 안 보는 monitoring과 meta를 아예 구독하지 않는 terminal은 같은 화면혼합 위험의 다른 소비자다. `/dev`의 same-window data-view-changed 이벤트는 mutually exclusive standalone route여서 실제 운영 중 자동으로 겹친다고 과장하지 않는다.


## A06 전체 파일 원장 및 분류 한계

목록은 A01의 66개를 빠짐없이 대응했다. 아래 :1은 파일 식별용 시작 위치이며 발견의 정확한 줄·심볼은 후보 카드에 적었다. “상위소비/표시symbol 확인”은 모든 내부 계산 분기의 정확성을 확인했다는 뜻이 아니다. source 전체 파일 목록과 실제 import/소비 경로를 조사했지만 모든 TAF/기상어휘/설정 조합의 전문 감사까지 끝내지 않았다.

| A01 파일 | 실제 역할·확인 깊이 |
|---|---|
| `frontend/src/features/monitoring/MonitoringMap.jsx:1` | MapView 실제 wrapper, 제한 layer/ring/slide; lifecycle 본문 추적 |
| `frontend/src/features/monitoring/MonitoringPage.css:1` | fixed canvas/map/overlay 스타일 소비; 관련 규칙만 판독, 전체 cascade W12 |
| `frontend/src/features/monitoring/MonitoringPage.jsx:1` | route의 root·polling·alert·설정·슬라이드·legacy 소비; lifecycle 본문 추적 |
| `frontend/src/features/monitoring/MonitoringSlideOverlay.jsx:1` | page/map wrapper의 2-layer fade·timer·content refs; 본문 추적 |
| `frontend/src/features/monitoring/MonitoringWxInfoSlide.jsx:1` | AirportInfoDocument fit/ResizeObserver/font-ready; 본문 추적 |
| `frontend/src/features/monitoring/legacy/App.css:1` | MonitoringPage 실제 import; signage visibility/reduced motion 관련 규칙, 나머지 cascade W12 |
| `frontend/src/features/monitoring/legacy/components/GroundCurrentWeatherCard.jsx:1` | ground current·AMOS/environment/METAR·sunrise·classic prop; 본문 주요 변환 추적 |
| `frontend/src/features/monitoring/legacy/components/GroundForecastClassic.jsx:1` | 현재 선택 가능한 classic 부모; 두 child 동시 렌더 |
| `frontend/src/features/monitoring/legacy/components/GroundForecastClassicPanel.jsx:1` | classic weekly/source/icon adapter; props·symbol·표시 가지 확인 |
| `frontend/src/features/monitoring/legacy/components/GroundForecastPanel.jsx:1` | signage weekly6칸·공용 WeatherIcon/아이콘 mapper; props·render 확인 |
| `frontend/src/features/monitoring/legacy/components/GroundForecastViewport.jsx:1` | signage hourly/weekly 12초 전환·hidden/inert; 본문 추적 |
| `frontend/src/features/monitoring/legacy/components/GroundHourlyStrip.jsx:1` | signage SVG8칸·temperature/precip·sunrise; props·selector 확인, geometry 미측정 |
| `frontend/src/features/monitoring/legacy/components/GroundHourlyStripClassic.jsx:1` | classic SVG/3시간 자료·별도 column 계약; props·symbol 확인 |
| `frontend/src/features/monitoring/legacy/components/Header.jsx:1` | 공항 dropdown document mousedown off; 실제 page header |
| `frontend/src/features/monitoring/legacy/components/MetarCard.jsx:1` | ops METAR/AMOS/shared 계산·WeatherIcon; props·주요 계산 확인, 모든 weather code 조합 미확인 |
| `frontend/src/features/monitoring/legacy/components/TafTimeline.jsx:1` | ops의 timeline/card/table·shared resolver/helpers·legacy isTestPage 가지; 진입/표시symbol 확인, 전체 TAF 조합 미확인 |
| `frontend/src/features/monitoring/legacy/components/WarningList.jsx:1` | airport/KMA entries·measurement/pages/rotation·빈ground 처리; 본문 추적 |
| `frontend/src/features/monitoring/legacy/components/alerts/AlertPanel.jsx:1` | validKeys/firstFired/sort/clock 조건부timer; 본문 추적 |
| `frontend/src/features/monitoring/legacy/components/alerts/AlertSound.jsx:1` | AudioContext/beep timer/played ids; 본문 추적 |
| `frontend/src/features/monitoring/legacy/components/alerts/Settings.jsx:1` | page 모달·inline local config와 apply/preview·ground탭제한; callback/표시symbol 확인, 전체 UX 미검증 |
| `frontend/src/features/monitoring/legacy/components/warningBannerModel.js:1` | WarningList source merge/label/KMA formal suffix; 본문 추적 |
| `frontend/src/features/monitoring/legacy/utils/advisory-filter.js:1` | MonitoringPage/Settings localStorage 필터 key adapter; 상위소비 확인, MapView 적용 후속 |
| `frontend/src/features/monitoring/legacy/utils/alerts/alert-dispatcher.js:1` | page callback·dispatch/quietHours/popup fields; 본문 주요 경로 |
| `frontend/src/features/monitoring/legacy/utils/alerts/alert-engine.js:1` | trigger enabled/category 판정/catch; 본문 추적 |
| `frontend/src/features/monitoring/legacy/utils/alerts/alert-settings.js:1` | defaults+localStorage merge·deprecated keys normalization; page/Settings 실제소비 확인 |
| `frontend/src/features/monitoring/legacy/utils/alerts/alert-state.js:1` | 공항별 stable keys/cooldown/firstFired/clearResolved; 본문 추적 |
| `frontend/src/features/monitoring/legacy/utils/alerts/alert-triggers.js:1` | 8개 METAR/TAF/lightning trigger·highlight data; 시간/이전/현재 대표 위험 추적 |
| `frontend/src/features/monitoring/legacy/utils/alerts/index.js:1` | alerts facade re-export, MonitoringPage 실제 import; 삭제 불가 이름만 legacy |
| `frontend/src/features/monitoring/legacy/utils/alerts/taf-change.js:1` | previous/current overlap·hard line·tail risk; engine→trigger→helper 추적, 모든 slot 조합 미검증 |
| `frontend/src/features/monitoring/legacy/utils/alerts/taf-risk.js:1` | enabled thresholds·slot risk mapper; 위 경로의 순수 계산 |
| `frontend/src/features/monitoring/legacy/utils/groundForecastIcons.js:1` | signage ground source vocab→registry mapper; classic과 차이 유지 검토 |
| `frontend/src/features/monitoring/legacy/utils/groundForecastTimer.js:1` | viewport timeout 소유/clear 반환; 본문 추적 |
| `frontend/src/features/monitoring/legacy/utils/groundForecastViewModel.js:1` | hourly8/weekly6 padding·scale/precip·source meta·12초 constants; 본문 추적 |
| `frontend/src/features/monitoring/legacy/utils/helpers.js:1` | legacy cards/alerts의 UTC 표시·weather 계산 adapter; 공유 함수 경계 확인, 모든 export 의미 전수 판정 아님 |
| `frontend/src/features/monitoring/legacy/utils/route-mode.js:1` | TafTimeline에서 isTestPage만 소비하는 옛 /test adapter; /monitoring은 ops 가지, 파일이름만 폐기 판단 금지 |
| `frontend/src/features/monitoring/legacy/utils/visual-mapper.js:1` | MetarCard/GroundCurrentWeatherCard 한국어 weather 변환; 상위소비 확인, vocab 전수 대조 미확인 |
| `frontend/src/features/monitoring/lib/airportSelection.js:1` | MonitoringPage 군공항 제외·available fallback; 본문 추적 |
| `frontend/src/features/monitoring/lib/canvasScale.js:1` | monitoring >=1200 조건부1920 scaling; 본문 추적 |
| `frontend/src/features/monitoring/lib/fitScale.js:1` | WxInfoSlide measured width search; 호출/조건 확인, font/geometry 측정 미실행 |
| `frontend/src/features/monitoring/lib/groundForecastDisplayMode.js:1` | signage/classic 저장값 normalization; 본문 추적 |
| `frontend/src/features/monitoring/lib/monitoringClock.js:1` | Intl explicit KST/UTC clock formatter; 본문 추적 |
| `frontend/src/features/monitoring/lib/monitoringSlideshow.js:1` | schedule/config migration/validation/IDB image persistence; 본문 주요 경로 |
| `frontend/src/features/monitoring/monitoringApi.js:1` | initial18 data+defaults·optional decode·hash diff/advance; 본문 추적 |
| `frontend/src/features/monitoring/useMonitoringSlideshow.js:1` | status/dwell timers·availability·objectURL revocation; 본문 추적 |
| `frontend/src/features/terminal/DestinationWeatherPage.jsx:1` | terminal root·8request polling·sourceadapter·dualqueue·전환/clock; 본문 추적 |
| `frontend/src/features/terminal/TerminalPage.jsx:1` | production entry import, fixture 설명과 live 경로 문서차이 |
| `frontend/src/features/terminal/WeatherFirstScreen.jsx:1` | weather3day strip·current flight·pending transition; props/source/render 확인 |
| `frontend/src/features/terminal/WeeklyWeatherScreen.jsx:1` | rail hourly+weekly+rolling flight list; timer/source/render 확인 |
| `frontend/src/features/terminal/assets/airport-weather-qr.svg:1` | terminalShared에서 정적 import 확인; 이미지 내용/생성·배포 전수는 W12 |
| `frontend/src/features/terminal/assets/amo-wordmark.png:1` | terminalShared에서 정적 import 확인; 이미지 내용/생성·배포 전수는 W12 |
| `frontend/src/features/terminal/assets/board-af.png:1` | 현재 frontend/src literal 소비 미발견; A12/W12 동적·문서·배포 참조 추가확인, 삭제판정 아님 |
| `frontend/src/features/terminal/assets/board-header-plane.png:1` | 현재 frontend/src literal 소비 미발견; A12/W12 동적·문서·배포 참조 추가확인, 삭제판정 아님 |
| `frontend/src/features/terminal/assets/board-jal.png:1` | 현재 frontend/src literal 소비 미발견; A12/W12 동적·문서·배포 참조 추가확인, 삭제판정 아님 |
| `frontend/src/features/terminal/assets/board-sq.png:1` | 현재 frontend/src literal 소비 미발견; A12/W12 동적·문서·배포 참조 추가확인, 삭제판정 아님 |
| `frontend/src/features/terminal/assets/forecast-cloud-transparent.png:1` | terminalShared에서 정적 import 확인; 이미지 내용/생성·배포 전수는 W12 |
| `frontend/src/features/terminal/assets/forecast-partly-transparent.png:1` | terminalShared에서 정적 import 확인; 이미지 내용/생성·배포 전수는 W12 |
| `frontend/src/features/terminal/assets/forecast-rain-transparent.png:1` | terminalShared에서 정적 import 확인; 이미지 내용/생성·배포 전수는 W12 |
| `frontend/src/features/terminal/assets/forecast-storm-transparent.png:1` | terminalShared에서 정적 import 확인; 이미지 내용/생성·배포 전수는 W12 |
| `frontend/src/features/terminal/terminal.css:1` | TerminalPage 실제 import; layout/selectors 확인, 전역 cascade/자산전수 W12 |
| `frontend/src/features/terminal/terminalAirportSelection.js:1` | terminal7공항/경로ICAO/catalog교집합; 본문 추적 |
| `frontend/src/features/terminal/terminalCanvasScale.js:1` | terminal 항상1920 canvas; monitoring의1200하한과 의도적 차이 |
| `frontend/src/features/terminal/terminalFlightSimulation.js:1` | datedfixture·live row adapter·두queue·slot transitions; 본문 주요 경로 추적 |
| `frontend/src/features/terminal/terminalForecastStrip.js:1` | 3day/24h8칸·날짜/강수단위; 본문 추적 |
| `frontend/src/features/terminal/terminalLiveData.js:1` | 8API optional·ICAO resolver·AMOS/METAR·예보 merge; 본문 추적 |
| `frontend/src/features/terminal/terminalShared.jsx:1` | source footer/clock/current·FlightRow/logo·settings events; 본문 주요 경로 |
| `frontend/src/features/terminal/terminalWeeklyForecast.js:1` | date normalize/5day rows/unknown padding; 본문 추적 |

### A06 밖에서 계약을 연결한 파일

| 파일·소유 | 읽은 경계 |
|---|---|
| `frontend/src/app/App.jsx:375` / A02 | standalone route/모바일redirect, providers |
| `frontend/src/app/useWeatherPolling.js:15` 및 `frontend/src/app/pollingData.js:1` / A02 | initial/poll/changed merge/error/timer 수명(W04 입력 심화) |
| `frontend/src/api/weatherApi.js:1` / A04 | fetchSnapshotMeta의 monitoring 연결; 내부 전체감사 아님 |
| `frontend/src/features/map/MapView.jsx:1` / A03 | MonitoringMap의 외부 props/ref 소비와 enable flag; 내부 수명은 W05 |
| `frontend/src/shared/weather/helpers.js:1`, `weather-visual-resolver.js:1`, `weather-icon-registry.js:1`, `frontend/src/shared/ui/WeatherIcon.jsx:1` / A04,A11,A12 | legacy와 terminal이 현재 사용하는 계산/시각어휘 경계 |
| `frontend/src/features/airport-panel/AirportInfoDocument.jsx:1` / A04 | wxinfo slide 문서 renderer 재사용 |
| `backend/server.js:90` / A04 | active root/ETag/sendLatest 및 관련 API routes |
| `backend/src/collector-registry.js:50`, `backend/src/index.js:149` / A10 | scheduler 등록·processor 연결·locks, 실행 없음 |
| `backend/src/processors/terminal-flight-processor.js:1` / A04 | KAC/IIAC validation, normalize/codeshares/이전 lookup·게시 |
| `backend/src/processors/ground-forecast-processor.js:461` / A04 | source/status/previous quality fallback/hourly 게시 |
| `backend/src/processors/overseas-forecast-processor.js:52`, `backend/src/processors/overseas-daily.js:1` / A04 | KST slot·daily 모델 경계(전체 날짜/offset 케이스 미검증) |
| `backend/src/processors/amos-processor.js:118` / A04 | terminal null/0 후보의 생산자 명시결측 |
| `backend/src/dev/data-view.js:45`, `backend/src/store.js:1` / A10 | active source revision/읽기·저장 계약(W03 입력); 운영 데이터는 읽거나 변경하지 않음 |
| `frontend/src/app/App.css:280` / A12 | reduced-motion important cascade |

### 연결된 테스트·기존 검증 목록 (이번 실행 없음)

A13로 분류되는 colocated test와 browser fixture는 A06 66파일 밖이다. 테스트의 존재/기대값 및 관련 실패 산출물을 읽었으며 아래 파일들이 이번에 전부 전문 검토되거나 통과했다는 뜻은 아니다.

- Colocated 30파일: `frontend/src/features/monitoring/MonitoringMap.test.js`, `frontend/src/features/monitoring/MonitoringSlideOverlay.test.js`, `frontend/src/features/monitoring/legacy/components/GroundCurrentWeatherCard.test.js`, `frontend/src/features/monitoring/legacy/components/GroundForecastPanel.signage.test.js`, `frontend/src/features/monitoring/legacy/components/alerts/Settings.test.js`, `frontend/src/features/monitoring/legacy/components/warningBannerModel.test.js`, `frontend/src/features/monitoring/legacy/utils/alerts/alert-dispatcher.test.js`, `frontend/src/features/monitoring/legacy/utils/alerts/alert-settings.test.js`, `frontend/src/features/monitoring/legacy/utils/alerts/alert-state.test.js`, `frontend/src/features/monitoring/legacy/utils/alerts/alert-triggers.test.js`, `frontend/src/features/monitoring/legacy/utils/alerts/taf-change.test.js`, `frontend/src/features/monitoring/legacy/utils/alerts/taf-risk.test.js`, `frontend/src/features/monitoring/legacy/utils/groundForecastIcons.test.js`, `frontend/src/features/monitoring/legacy/utils/groundForecastTimer.test.js`, `frontend/src/features/monitoring/legacy/utils/groundForecastViewModel.test.js`, `frontend/src/features/monitoring/legacy/utils/helpers.test.js`, `frontend/src/features/monitoring/lib/airportSelection.test.js`, `frontend/src/features/monitoring/lib/canvasScale.test.js`, `frontend/src/features/monitoring/lib/fitScale.test.js`, `frontend/src/features/monitoring/lib/groundForecastDisplayMode.test.js`, `frontend/src/features/monitoring/lib/monitoringClock.test.js`, `frontend/src/features/monitoring/lib/monitoringSlideshow.test.js`, `frontend/src/features/monitoring/monitoringApi.test.js`, `frontend/src/features/terminal/DestinationWeatherPage.board-layout.test.js`, `frontend/src/features/terminal/terminalAirportSelection.test.js`, `frontend/src/features/terminal/terminalCanvasScale.test.js`, `frontend/src/features/terminal/terminalFlightSimulation.test.js`, `frontend/src/features/terminal/terminalForecastStrip.test.js`, `frontend/src/features/terminal/terminalLiveData.test.js`, `frontend/src/features/terminal/terminalWeeklyForecast.test.js`.
- Browser 관련: `frontend/verification/contracts/monitoring-visual.spec.mjs`, `monitoring-ground-signage.spec.mjs`, `terminal-signage.spec.mjs`, `frontend/verification/monitoring-fixture.mjs`, `monitoring-ground-signage-fixture.mjs`; alert/slideshow 계약은 registry에 따라 주 담당이 좁은 ID로 선택한다. 경보 시간·큐 갱신·부분 실패는 위 후보의 검증 조건을 우선한다.
- Producer 관련: terminal-flight/ground-forecast/overseas-forecast의 기존 test는 정규화·fallback·codeshare 경계의 재사용 검증 후보. 스케줄과 실제 upstream 호출·저장까지 통과를 주장하지 않는다.

## 실제 수행·미수행 기록

- 수행: `git rev-parse HEAD`, `git status --short`; `rg --files`/`rg -n`로 전체 A06 import·timer·event·fetch·소비symbol 검색; `nl -ba`/`sed`/`cat`으로 정책·소스·기존 tests·browser error-context/console 읽기; Python 표준라이브러리로 inventory JSON/NUL 추적 목록 분류와 W08.md 원장 작성. `view_image`로 기존 ops actual/expected PNG 2장을 열어 비교했다.
- 산출물: **W08.md만 작성**. CP1→CP2→후보/원장→마지막 반환 위치를 같은 파일에 보존했다. 기존 W02/W05 보고서와 공통 문서·소스·설정·영구 테스트·실황 데이터는 수정하지 않았다.
- 미수행: npm test/check/build, contract 실행, HTTP 요청·서버·수집·배포, 신규 브라우저/스크린샷/성능측정, 추가 에이전트 생성 모두 없음. 후보의 수치효과나 실제 운영빈도는 없음.
- 문서 점검 결과: UTF-8 decode 성공, CR 0(LF), A01의66파일 원장 누락0, 명시한 실제 파일 경로 누락0. 현재 HEAD가 기준과 같고 git status가 기존 AGENTS/CLAUDE/docs/README 수정 및 docs/refactoring 미추적 상태로 유지됨을 읽기로 확인했다. 이는 앱 기능 테스트가 아니다.

## 마지막 체크포인트 / 남은 범위와 다음 좁은 질문

- 반환 상태: RF-002-W08의 **독립 진단 1묶음 완료·A06 부분 확인**. 9개 source 후보, 8개 W12/검증·자산 반환, 3개 producer/source 질문을 임시 ID로 제시했으며 전역OBS 연결·선정·구현·공통문서 반영은 주 담당 소유다.
- 재개 위치: W08-03의 부모 queue integration과 W08-05의 결측 변환부터 좁게 검증하면 소스 조건이 가장 명확하다. W08-01/W08-04의 공통 polling 계약은 W04 결과와 합쳐 결정한다. 이 순서는 구현 승인이나 검사 실행이 아니다.
- 남은 범위: legacy helpers/visual-mapper/TAF 표시 전 분기·날씨 코드 전수; 모든 Settings 항목의 실제 MapView filter 적용; 전체 CSS 우선순위/토큰/반응형/다크모드와 자산 생성·배포; 모든 source 조합·코드셰어 collision·운항일/자정 상태; 장시간 resource 수·실 네트워크/서버 실패·자동복구 실측; QR/로고 콘텐츠·링크 접근성. 전체파일 목록 확보를 이 범위의 기능검증 완료로 보지 않는다.
- 좁은 후속 질문 Q1: 4개의 board frame 중 cursor1에서 rail로 전환한 채 두 번 poll하면 최신 gate/delay가 어느 소비자에서 적용되는가, frame순서와 row값의 허용 갱신 경계는 각각 무엇인가?
- Q2: production terminal의 flights503/METAR503은 고정fixture, 마지막 실제값, 또는 명시무자료 중 무엇을 보여야 하며 source/reference 시각은 어디에 표시할 것인가? validempty[]는 별도 유지한다.
- Q3: alert-defaults 최초 실패와 optional JSON decode 실패에서 최소 화면과 자동retry를 시작할 수 있는 조건은 무엇인가? W04 initialError reset과 함께 확정한다.
- Q4: signage의 활성 공항경보는 8/11 유지·8/12 숨김 중 어떤 계약이 현재 유효한가? W12가 CSS와 검증을 정렬하기 전에 A15 의도 이력 확인이 필요하다.
- Q5: 동일 snapshot의 quiet 종료/TAF lookahead 진입을 몇 초 또는 몇 분 내 평가해야 하는가, 선택TZ와 OS local schedule의 관계는 무엇인가?
- Q6: 현재 terminal 지원7공항의 지연편은 scheduled가 지난 뒤 언제까지 표시해야 하는가, snapshot의 fetched_at과 실제운항일은 어떻게 구분하는가?
- Q7: WarningList가 읽지 않는 groundOverview, 저장만 보이는 traffic/advisory 개인필터, 네 개 board 이미지의 다른 동적/배포 소비자가 있는가? 모니터링 제한 layer profile의 의도와 A12 전수 참조로 좁혀 확인한다.
- 마지막 보고서 작성 시각: 2026-09-12T01:28:48.829738+09:00. 최종 coordinator check 후 현 dispatch에 worker_done을 한 번 보내고 idle로 종료한다.
