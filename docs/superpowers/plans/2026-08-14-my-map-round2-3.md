# 내 지도 2판·3판 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 2판 — 지점을 파일이 가리키는 아이콘으로 그리고 눌러서 설명을 본다. 3판 — 공역 기둥과 뜬 경로를 세우고 고도를 과장해 본다.

**Architecture:** 1판이 만든 `features/my-map/`에 얹는다. 계산은 순수 모듈로 빼서 `node --test`로 굳히고, `useMyMap`은 레이어를 더할 뿐이다. 3판 재료(`kmlWalls.js`)는 스파이크에서 시험 19개로 이미 굳어 있으므로 옮겨서 배선만 한다.

**Tech Stack:** React, Mapbox GL JS 3.23.1 (`loadImage`/`addImage`, `fill-extrusion`, `line-z-offset`), `node --test`, Playwright.

**Spec:** `docs/superpowers/specs/2026-08-14-my-map-design.md` (「범위 밖 → 다음 판으로 미루는 것」)
**1판 계획:** `docs/superpowers/plans/2026-08-14-my-map-round1.md`
**측정 근거:** `docs/superpowers/status/2026-08-12-kml-viewer-spike-findings.md`

## Global Constraints

- Linux 전용. `npm`/`node`/`git`은 Linux 셸에서만 실행한다.
- 사용자 노출 문구는 한국어. 비ASCII 편집 전 [encoding safety](../../policies/encoding-safety.md)를 읽는다.
- **새 의존성을 추가하지 않는다.**
- 스타일은 파일이 정한 값을 쓴다. 값이 없을 때만 기본값을 쓴다.
- **어떤 도형도 숨기지 않는다.**
- 그리기 순서: 기상 > 내 지도 > 지형 근접 > 기본 지도. KML 레이어는 `middle` 슬롯.
- `features/my-map/` 밖은 이 계획이 명시한 파일만 수정한다.
- **설명 글은 남이 만든 파일에서 온다. 절대 HTML로 넣지 않는다** — 글자로만 그린다.
- 각 태스크는 `cd frontend && node --test <파일>`이 통과한 뒤에만 커밋한다. 전체는 `npm test`.
- 커밋 전 `git status`로 브랜치를 확인하고 **이 계획이 만든 파일만** `git add` 한다. `git add -A`를 쓰지 않는다.

## 확정된 결정

| 무엇 | 정한 것 | 왜 |
| --- | --- | --- |
| 아이콘 vs 원 | **아이콘이 있으면 아이콘만**, 없거나 못 불러오면 원 | 구글어스와 가장 가깝고 화면이 깨끗하다 |
| 설명 표시 | **지도 위 풍선** | 구글어스와 같은 방식, 어느 도형인지 바로 안다 |
| 3D 스위치 자리 | **내 지도 패널 안** | 지금은 내 지도 레이어만 3D가 된다. 지형 근접까지 3D로 갈 때 지도 쪽으로 옮긴다 |

## File Structure

| 파일 | 책임 |
| --- | --- |
| `frontend/src/features/my-map/lib/kmlIcons.js` (신규) | 아이콘 주소 모으기·id 만들기. 순수 |
| `frontend/src/features/my-map/lib/kmlWalls.js` (이동) | 벽→기둥, 오르내리는 선→뜬 선, 고도 과장. 시험 19개 포함 |
| `frontend/src/features/my-map/lib/cameraLimit.js` (신규) | 과장 배수별 최대 확대. 순수 |
| `frontend/src/features/my-map/lib/kmlPaint.js` (수정) | 라벨 레이어가 아이콘도 그린다 |
| `frontend/src/features/my-map/useMyMap.js` (수정) | 아이콘 등록, 풍선, 기둥·뜬 선, 3D·과장·카메라 |
| `frontend/src/features/my-map/MyMapPanel.jsx` / `.css` (수정) | 3D 스위치, 고도 과장 선택 |
| `frontend/verification/contracts/my-map.spec.mjs` (수정) | 계약 추가 |
| `docs/policies/verification/contracts.md` (수정) | 등록부 갱신 |

---

# 2판 — 구글어스에서 보던 그대로

### Task 1: 지점 아이콘

스파이크에서 **37종 전부 지도 등록에 성공**했다(549ms). 파일이 가리키는 주소는 40종인데 1종은 KMZ 안 상대경로(`files/dme1.bmp`)이고 그 그림은 압축에 없다 — `httpsIcon`이 그런 것에 `null`을 주므로 자동으로 원으로 남는다.

**Files:**
- Create: `frontend/src/features/my-map/lib/kmlIcons.js`
- Test: `frontend/src/features/my-map/lib/kmlIcons.test.js`
- Modify: `frontend/src/features/my-map/lib/kmlPaint.js`, `frontend/src/features/my-map/lib/kmlPaint.test.js`
- Modify: `frontend/src/features/my-map/useMyMap.js`

**Interfaces:**
- Consumes: `httpsIcon(url)` (`kmlPaint.js`)
- Produces:
  - `iconIdFor(url)` → `string` — 주소 하나에 대응하는 지도 이미지 id
  - `collectIconUrls(list)` → `Array<{ url, id }>` — 레이어 목록에서 쓸 수 있는 아이콘만 중복 없이
  - `LABEL_LAYOUT`에 `icon-image`·`icon-size`·`icon-anchor` 추가
  - `CIRCLE_FILTER_EXTRA` = `['!', ['has', '__icon']]` — 아이콘이 붙은 지점은 원을 그리지 않는다

- [ ] **Step 1: 실패하는 시험 작성**

`frontend/src/features/my-map/lib/kmlIcons.test.js`:

```js
import test from 'node:test'
import assert from 'node:assert/strict'
import { iconIdFor, collectIconUrls } from './kmlIcons.js'

const feat = (icon) => ({ type: 'Feature', properties: icon ? { icon } : {}, geometry: { type: 'Point', coordinates: [127, 37] } })
const layer = (id, ...features) => ({ id, name: id, depth: 0, parentId: null, features })

test('같은 주소는 늘 같은 id를 준다', () => {
  const a = iconIdFor('https://maps.google.com/mapfiles/kml/paddle/red-circle.png')
  const b = iconIdFor('https://maps.google.com/mapfiles/kml/paddle/red-circle.png')
  assert.equal(a, b)
})

test('다른 주소는 다른 id를 준다', () => {
  assert.notEqual(
    iconIdFor('https://maps.google.com/mapfiles/kml/paddle/red-circle.png'),
    iconIdFor('https://maps.google.com/mapfiles/kml/paddle/blu-circle.png'),
  )
})

test('id는 지도가 받아들이는 글자만 쓴다', () => {
  // 주소에는 슬래시·물음표·한글이 섞일 수 있다. 그대로 id로 쓰면 안 된다.
  const id = iconIdFor('https://예시.kr/아이콘 모음/a b?c=1&d=2.png')
  assert.match(id, /^my-map-icon-[a-z0-9]+$/)
})

test('http 주소는 https로 바꿔 모은다', () => {
  const out = collectIconUrls([layer('f0', feat('http://maps.google.com/mapfiles/kml/paddle/A.png'))])
  assert.equal(out.length, 1)
  assert.equal(out[0].url, 'https://maps.google.com/mapfiles/kml/paddle/A.png')
})

test('같은 주소가 여러 번 나와도 한 번만 모은다', () => {
  const url = 'https://maps.google.com/mapfiles/kml/paddle/A.png'
  const out = collectIconUrls([layer('f0', feat(url), feat(url)), layer('f1', feat(url))])
  assert.equal(out.length, 1)
})

test('압축 안 상대경로는 쓸 수 없으므로 빼고 모은다', () => {
  // 맥케이 파일의 files/dme1.bmp가 이렇다 — 게다가 그 그림은 압축에 들어 있지도 않다.
  const out = collectIconUrls([layer('f0', feat('files/dme1.bmp'), feat(undefined))])
  assert.deepEqual(out, [])
})

test('id가 주소와 짝이 맞는다', () => {
  const url = 'https://maps.google.com/mapfiles/kml/paddle/A.png'
  const out = collectIconUrls([layer('f0', feat(url))])
  assert.equal(out[0].id, iconIdFor(url))
})
```

- [ ] **Step 2: 시험이 실패하는지 확인**

```bash
cd frontend && node --test src/features/my-map/lib/kmlIcons.test.js
```
Expected: FAIL — 모듈이 없다.

- [ ] **Step 3: 구현**

`frontend/src/features/my-map/lib/kmlIcons.js`:

```js
import { httpsIcon } from './kmlPaint.js'

// 파일이 가리키는 아이콘 주소를 모아 지도에 올릴 준비를 한다.
//
// 스파이크에서 37종 전부 등록에 성공했다(549ms). 주소는 대부분 구글 어스 기본
// 아이콘이라 http로 적혀 있고, https 페이지에서는 차단되므로 httpsIcon이 주소만
// 바꾼다. 압축 안 상대경로는 쓸 수 없어 null이 되고, 그런 지점은 원으로 남는다.

// 주소를 그대로 id로 쓸 수 없다 — 슬래시·물음표·한글이 섞인다. 짧고 안전한
// 글자로 접는다. 충돌은 서로 다른 주소가 같은 값으로 접힐 때만 생기는데, 한 파일이
// 쓰는 아이콘은 수십 종이라 실질적으로 일어나지 않는다.
export function iconIdFor(url) {
  let hash = 0x811c9dc5
  const text = String(url)
  for (let i = 0; i < text.length; i += 1) {
    hash ^= text.charCodeAt(i)
    hash = Math.imul(hash, 0x01000193) >>> 0
  }
  return `my-map-icon-${hash.toString(36)}${text.length.toString(36)}`
}

export function collectIconUrls(list) {
  const byUrl = new Map()
  for (const layer of list ?? []) {
    for (const f of layer.features ?? []) {
      const url = httpsIcon(f.properties?.icon)
      if (url && !byUrl.has(url)) byUrl.set(url, { url, id: iconIdFor(url) })
    }
  }
  return [...byUrl.values()]
}
```

- [ ] **Step 4: 시험 통과 확인**

```bash
cd frontend && node --test src/features/my-map/lib/kmlIcons.test.js
```
Expected: PASS — 7개.

- [ ] **Step 5: 라벨 레이어가 아이콘도 그리게 한다**

이름표 레이어는 이미 지점만 골라 그리는 심벌 레이어다. 아이콘 레이어를 따로 만들면 두 심벌 레이어가 서로 자리를 다투므로, **같은 레이어에 아이콘과 글자를 함께** 둔다.

`frontend/src/features/my-map/lib/kmlPaint.js`의 `LABEL_LAYOUT`을 이렇게 바꾼다:

```js
export const LABEL_LAYOUT = {
  // 아이콘과 이름표를 한 레이어에 둔다. 따로 두면 두 심벌 레이어가 겹침 계산을
  // 각자 해서 아이콘 옆에 남의 이름표가 끼어든다.
  'icon-image': ['coalesce', ['get', '__icon'], ''],
  // 구글 어스 아이콘은 64px 안팎이라 그대로 쓰면 지도를 덮는다. 파일의 icon-scale을
  // 존중하되 절반으로 줄여 얹는다.
  'icon-size': ['*', ['coalesce', ['get', 'icon-scale'], 1], 0.5],
  // 압정·깃발은 아래쪽 끝이 그 지점을 가리킨다.
  'icon-anchor': 'bottom',
  'icon-allow-overlap': false,
  'text-field': ['coalesce', ['get', 'name'], ''],
  // 파일의 label-scale을 존중하되 바닥을 둔다. 맥케이 파일의 IC·JC 이름은 0.7이라
  // 예전 기준(11px)으로 7.7px이었다 — 화면에서 읽을 수 없는 크기다.
  // 바닥을 너무 올리면 작게 그리라고 지정한 것들이 오히려 제일 도드라진다.
  'text-size': ['max', ['*', ['coalesce', ['get', 'label-scale'], 1], 13], 10],
  'text-offset': [0, 0.6],
  'text-anchor': 'top',
  'text-allow-overlap': false,
  'text-optional': true,
}

// 아이콘이 붙은 지점은 원을 그리지 않는다 — 압정 밑에 색 동그라미가 깔리면 지저분하다.
export const CIRCLE_FILTER_EXTRA = ['!', ['has', '__icon']]
```

`frontend/src/features/my-map/lib/kmlPaint.test.js`에 추가한다:

```js
test('아이콘과 이름표를 한 레이어에 둔다', () => {
  assert.deepEqual(LABEL_LAYOUT['icon-image'], ['coalesce', ['get', '__icon'], ''])
  assert.deepEqual(LABEL_LAYOUT['icon-size'], ['*', ['coalesce', ['get', 'icon-scale'], 1], 0.5])
  assert.equal(LABEL_LAYOUT['icon-anchor'], 'bottom')
})

test('아이콘이 있으면 원을 그리지 않는다', () => {
  assert.deepEqual(CIRCLE_FILTER_EXTRA, ['!', ['has', '__icon']])
})

test('아이콘이 자리를 못 잡아도 이름표는 나온다', () => {
  // text-optional이 없으면 아이콘이 밀려날 때 이름표까지 사라진다.
  assert.equal(LABEL_LAYOUT['text-optional'], true)
})
```

시험 파일 맨 위 import에 `CIRCLE_FILTER_EXTRA`를 더한다.

- [ ] **Step 6: 시험 통과 확인**

```bash
cd frontend && node --test "src/features/my-map/lib/*.test.js"
```
Expected: PASS — 기존 것 + 신규 10개.

- [ ] **Step 7: 지도에 배선**

`frontend/src/features/my-map/useMyMap.js`:

import 두 줄을 더한다:

```js
import { LINE_PAINT, FILL_PAINT, CIRCLE_PAINT, LABEL_LAYOUT, LABEL_PAINT, labelHaloFor, CIRCLE_FILTER_EXTRA } from './lib/kmlPaint.js'
import { collectIconUrls, iconIdFor } from './lib/kmlIcons.js'
```

`LAYER_DEFS`의 circle 항목에 필터를 더한다:

```js
  { kind: 'circle', type: 'circle', geom: ['all', ['==', ['geometry-type'], 'Point'], CIRCLE_FILTER_EXTRA], paint: CIRCLE_PAINT },
```

훅 안, `rebuild` 바로 위에 아이콘을 올리는 함수를 둔다:

```js
  // 지도에 이미 올린 아이콘. 파일을 껐다 켜도 다시 받지 않는다.
  const loadedIconsRef = useRef(new Set())

  // 아이콘을 지도에 등록한다. 실패한 것은 그냥 두면 그 지점이 원으로 남는다.
  const ensureIcons = useCallback(async (list) => {
    const map = mapRef.current
    if (!map) return
    const wanted = collectIconUrls(list).filter(({ id }) => !loadedIconsRef.current.has(id))
    if (wanted.length === 0) return
    await Promise.all(wanted.map(({ url, id }) => new Promise((resolve) => {
      // mapbox-gl의 loadImage는 콜백 방식이다.
      map.loadImage(url, (error, image) => {
        if (!error && image && !map.hasImage(id)) {
          try { map.addImage(id, image) } catch { /* 이미 올라와 있으면 그만이다 */ }
        }
        if (!error && image) loadedIconsRef.current.add(id)
        resolve()
      })
    })))
  }, [mapRef])
```

`rebuild` 안, feature에 속성을 심는 자리에 아이콘 id를 더한다:

```js
        for (const f of layer.features) {
          const iconUrl = httpsIcon(f.properties?.icon)
          const iconId = iconUrl ? iconIdFor(iconUrl) : null
          features.push({
            ...f,
            properties: {
              ...f.properties,
              __file: fileId,
              __folder: layer.id,
              // 글자색은 파일 것을 쓰고, 뒤에 깔리는 후광만 읽히도록 반대로 둔다.
              __labelHalo: labelHaloFor(f.properties?.['label-color']),
              // 지도에 실제로 올라간 아이콘만 심는다. 못 올라간 것은 원으로 남는다.
              ...(iconId && loadedIconsRef.current.has(iconId) ? { __icon: iconId } : {}),
            },
          })
        }
```

`httpsIcon`도 import에 더한다(`kmlPaint.js`에서).

`openFile`에서 목록을 만든 뒤, 상태를 바꾸기 **전에** 아이콘을 올린다 — 순서가 바뀌면 첫 그리기에 아이콘이 빠진다:

```js
    setBusy('아이콘 불러오는 중…')
    await ensureIcons(parsed.list)
    setBusy('지도에 올리는 중…')
```

`openFile`의 `useCallback` 의존성에 `ensureIcons`를 더한다.

- [ ] **Step 8: 빌드와 전체 시험**

```bash
cd frontend && npm run build 2>&1 | tail -2 && npm test 2>&1 | tail -5
```
Expected: 빌드 성공, 시험 전부 통과.

- [ ] **Step 9: 눈으로 확인**

[dev-server 절차](../../operations/dev-server-and-capture.md)로 띄우고 맥케이 파일을 올린 뒤 사용자에게 넘긴다:

> 지점이 압정·깃발·과녁 모양으로 나오나요? 아이콘이 없는 지점만 동그라미인가요?
> 아이콘이 지나치게 크거나 작지 않나요? 이름표가 아이콘에 가리지 않나요?

**기록할 값:** 아이콘 크기 느낌, 이름표 겹침 여부. 크기가 안 맞으면 `icon-size`의 0.5를 조정한다 — 이 값은 눈으로 맞추는 값이다.

- [ ] **Step 10: 커밋**

```bash
cd /home/john_doe/ProjectAMO && git branch --show-current && git status --short
git add frontend/src/features/my-map/lib/kmlIcons.js frontend/src/features/my-map/lib/kmlIcons.test.js frontend/src/features/my-map/lib/kmlPaint.js frontend/src/features/my-map/lib/kmlPaint.test.js frontend/src/features/my-map/useMyMap.js
git commit -m "feat(my-map): draw points with the icons the file points at"
```

---

### Task 2: 설명 풍선

`description`은 **남이 만든 파일에서 온 글**이다. 맥케이 파일의 것에도 HTML이 섞여 있다. **절대 HTML로 넣지 않는다** — `textContent`로만 그린다. 이건 줄일 수 있는 코너가 아니다.

클릭 판정 비용은 스파이크에서 쟀다: 중앙값 0.4ms, 최악 3.4ms.

**Files:**
- Modify: `frontend/src/features/my-map/useMyMap.js`
- Modify: `frontend/src/features/my-map/MyMapPanel.css` (풍선 모양)

**Interfaces:**
- Consumes: Task 1의 레이어들
- Produces: 없음 (지도 위 동작)

- [ ] **Step 1: 구현**

`frontend/src/features/my-map/useMyMap.js` — 아이콘 배선 아래에 붙인다:

```js
  // 도형을 누르면 파일에 적힌 이름과 설명을 보여준다.
  //
  // 설명은 남이 만든 파일에서 온 글이다. HTML로 넣으면 그 파일이 우리 화면에서
  // 무엇이든 실행할 수 있다. 요소를 만들어 textContent로만 채운다 — 문자열을
  // 이어붙여 setHTML을 부르는 길은 열어두지 않는다.
  useEffect(() => {
    const map = mapRef.current
    if (!map || !isStyleReady) return undefined
    const layers = LAYER_DEFS.map((d) => LYR(d.kind))

    const popup = new mapboxgl.Popup({ closeButton: true, maxWidth: '280px', className: 'my-map-popup' })

    const onClick = (e) => {
      const feature = e.features?.[0]
      if (!feature) return
      const name = feature.properties?.name
      const description = feature.properties?.description
      if (!name && !description) return

      const root = document.createElement('div')
      if (name) {
        const title = document.createElement('div')
        title.className = 'my-map-popup-title'
        title.textContent = String(name)
        root.append(title)
      }
      if (description) {
        const body = document.createElement('div')
        body.className = 'my-map-popup-body'
        // 파일이 준 글을 글자 그대로만 그린다.
        body.textContent = String(description)
        root.append(body)
      }
      popup.setLngLat(e.lngLat).setDOMContent(root).addTo(map)
    }
    const onEnter = () => { map.getCanvas().style.cursor = 'pointer' }
    const onLeave = () => { map.getCanvas().style.cursor = '' }

    map.on('click', layers, onClick)
    map.on('mouseenter', layers, onEnter)
    map.on('mouseleave', layers, onLeave)
    return () => {
      map.off('click', layers, onClick)
      map.off('mouseenter', layers, onEnter)
      map.off('mouseleave', layers, onLeave)
      popup.remove()
    }
  }, [mapRef, isStyleReady])
```

- [ ] **Step 2: 풍선 모양**

`frontend/src/features/my-map/MyMapPanel.css` 끝에 더한다:

```css
/* 지도 위 풍선. 패널과 같은 언어를 쓴다. */
.my-map-popup .mapboxgl-popup-content {
  padding: 10px 12px;
  border-radius: 10px;
  font-family: var(--app-font, system-ui, sans-serif);
}
.my-map-popup-title { font-size: 13px; font-weight: 600; margin-bottom: 4px; }
.my-map-popup-body { font-size: 12.5px; line-height: 1.55; white-space: pre-wrap; word-break: break-word; }
```

- [ ] **Step 3: 빌드와 전체 시험**

```bash
cd frontend && npm run build 2>&1 | tail -2 && npm test 2>&1 | tail -5
```
Expected: 빌드 성공, 시험 전부 통과.

- [ ] **Step 4: 눈으로 확인**

맥케이 파일을 올리고 제주 쪽 `물찻오름`·`위미항` 지점을 눌러 본다. 사용자에게 넘긴다:

> 지점을 누르면 이름과 설명이 뜨나요? 공역 면을 눌러도 뜨나요?
> 설명이 없는 도형을 누르면 아무 일도 안 일어나나요?

- [ ] **Step 5: 커밋**

```bash
cd /home/john_doe/ProjectAMO && git branch --show-current && git status --short
git add frontend/src/features/my-map/useMyMap.js frontend/src/features/my-map/MyMapPanel.css
git commit -m "feat(my-map): show the file's own description when a shape is tapped"
```

---

# 3판 — 3D

### Task 3: 3D 재료를 my-map으로 옮긴다

스파이크가 만들어 시험 19개로 굳혀 둔 `kmlWalls.js`를 새 자리로 옮긴다. 1판 Task 1과 같은 방식이다 — 복사가 아니라 이동이다.

**Files:**
- Move: `frontend/src/features/kml-viewer/lib/kmlWalls.js` (+ `.test.js`) → `frontend/src/features/my-map/lib/`
- Modify: `frontend/src/features/kml-viewer/useKmlMap.js`

**Interfaces:**
- Produces:
  - `wallToExtrusion(feature)` → `Feature|null` (바닥 고리 + `__base`·`__height`)
  - `buildWalls(list)` → `Feature[]`
  - `extrusionPaint(x)` → 페인트 객체 (x = 고도 과장 배수)
  - `lineToElevated(feature)` → `Feature[]` (`__elev` 배열)
  - `buildElevatedLines(list)` → `Feature[]`
  - `elevatedLineLayout(x)` → 레이아웃 객체

- [ ] **Step 1: 파일을 옮긴다**

```bash
cd /home/john_doe/ProjectAMO/frontend
git mv src/features/kml-viewer/lib/kmlWalls.js      src/features/my-map/lib/kmlWalls.js
git mv src/features/kml-viewer/lib/kmlWalls.test.js src/features/my-map/lib/kmlWalls.test.js
rmdir src/features/kml-viewer/lib 2>/dev/null || true
```

- [ ] **Step 2: 시험 19개가 그대로 통과하는지 확인**

```bash
cd frontend && node --test src/features/my-map/lib/kmlWalls.test.js
```
Expected: PASS — 19개.

- [ ] **Step 3: 스파이크 페이지의 import를 돌린다**

`frontend/src/features/kml-viewer/useKmlMap.js`:

```js
import { buildWalls, extrusionPaint, buildElevatedLines, elevatedLineLayout } from '../my-map/lib/kmlWalls.js'
```

- [ ] **Step 4: 빌드와 전체 시험**

```bash
cd frontend && npm run build 2>&1 | tail -2 && npm test 2>&1 | tail -5
grep -rn "kml-viewer/lib/kmlWalls" src/ || echo "남은 옛 경로 없음"
```
Expected: 전부 통과, 남은 경로 없음.

- [ ] **Step 5: 커밋**

```bash
cd /home/john_doe/ProjectAMO && git branch --show-current && git status --short
git add frontend/src/features/my-map/lib/kmlWalls.js frontend/src/features/my-map/lib/kmlWalls.test.js frontend/src/features/kml-viewer/useKmlMap.js
git commit -m "refactor(my-map): move the 3D pieces next to the feature that uses them"
```

---

### Task 4: 카메라 최소 거리

과장을 켜고 가까이 가면 **카메라가 기둥 안에 갇힌다.** 스파이크에서 잰 값:

| 카메라 | 1배 → 10배, 화면 덮는 비율 | 판정 |
| --- | --- | --- |
| zoom 8.5, pitch 55 | 47.5 % → 55.7 % | 잘 보인다 |
| zoom 9.5, pitch 65 | 56.1 % → 78.2 % | 보이지만 빽빽 |
| zoom 10.5, pitch 72 | 73.4 % → 83.7 % | 갇힌다 |

배수가 클수록 더 물러나야 한다. 표로 정하고 순수 함수로 굳힌다.

**Files:**
- Create: `frontend/src/features/my-map/lib/cameraLimit.js`
- Test: `frontend/src/features/my-map/lib/cameraLimit.test.js`

**Interfaces:**
- Produces: `maxZoomFor(exaggeration)` → `number`, `EXAGGERATION_STEPS` = `[1, 3, 5, 10, 20]`

- [ ] **Step 1: 실패하는 시험 작성**

`frontend/src/features/my-map/lib/cameraLimit.test.js`:

```js
import test from 'node:test'
import assert from 'node:assert/strict'
import { maxZoomFor, EXAGGERATION_STEPS } from './cameraLimit.js'

test('과장을 안 걸면 지도 기본 한계를 그대로 쓴다', () => {
  assert.equal(maxZoomFor(1), 16)
})

test('배수가 커질수록 더 물러나야 한다', () => {
  const zooms = EXAGGERATION_STEPS.map(maxZoomFor)
  for (let i = 1; i < zooms.length; i += 1) {
    assert.ok(zooms[i] < zooms[i - 1], `${EXAGGERATION_STEPS[i]}배가 ${EXAGGERATION_STEPS[i - 1]}배보다 더 물러나야 한다`)
  }
})

test('10배에서는 갇히지 않는 거리까지 물러난다', () => {
  // 실측: zoom 10.5에서 갇히고 8.5에서 잘 보였다.
  assert.ok(maxZoomFor(10) <= 9, '10배는 zoom 9 이하로 제한해야 한다')
})

test('표에 없는 배수는 가장 가까운 아래 단계를 따른다', () => {
  assert.equal(maxZoomFor(7), maxZoomFor(5))
  assert.equal(maxZoomFor(100), maxZoomFor(20))
})

test('이상한 값에도 던지지 않는다', () => {
  assert.equal(maxZoomFor(0), 16)
  assert.equal(maxZoomFor(-3), 16)
  assert.equal(maxZoomFor(null), 16)
  assert.equal(maxZoomFor('열배'), 16)
})
```

- [ ] **Step 2: 시험이 실패하는지 확인**

```bash
cd frontend && node --test src/features/my-map/lib/cameraLimit.test.js
```
Expected: FAIL — 모듈이 없다.

- [ ] **Step 3: 구현**

`frontend/src/features/my-map/lib/cameraLimit.js`:

```js
// 고도를 과장하면 기둥이 높아진다. 그 상태로 가까이 가면 카메라가 기둥 안에
// 들어가 화면이 온통 반투명 청록이 된다 — 표시가 깨진 게 아니라 덩어리 안에
// 있는 것이다. 배수마다 "이 이상 가까이 가면 갇힌다"는 선을 둔다.
//
// 값은 스파이크 실측에서 나왔다(zoom 10.5에서 갇히고 8.5에서 잘 보였다).
// ponytail: 눈으로 맞춘 표다. 화면 비율이나 기울기 기본값이 바뀌면 다시 재야 한다.
const LIMITS = [
  { x: 1, maxZoom: 16 },   // 지도 기본 한계 (MAP_CONFIG.maxZoom)
  { x: 3, maxZoom: 11 },
  { x: 5, maxZoom: 10 },
  { x: 10, maxZoom: 8.5 },
  { x: 20, maxZoom: 8 },
]

export const EXAGGERATION_STEPS = LIMITS.map((l) => l.x)

export function maxZoomFor(exaggeration) {
  const x = Number(exaggeration)
  if (!Number.isFinite(x) || x <= 1) return LIMITS[0].maxZoom
  let found = LIMITS[0]
  for (const limit of LIMITS) {
    if (x >= limit.x) found = limit
  }
  return found.maxZoom
}
```

- [ ] **Step 4: 시험 통과 확인**

```bash
cd frontend && node --test src/features/my-map/lib/cameraLimit.test.js
```
Expected: PASS — 5개.

- [ ] **Step 5: 커밋**

```bash
cd /home/john_doe/ProjectAMO && git branch --show-current && git status --short
git add frontend/src/features/my-map/lib/cameraLimit.js frontend/src/features/my-map/lib/cameraLimit.test.js
git commit -m "feat(my-map): keep the camera outside the columns it exaggerates"
```

---

### Task 5: 기둥과 뜬 경로를 지도에 올린다

**Files:**
- Modify: `frontend/src/features/my-map/useMyMap.js`

**Interfaces:**
- Consumes: Task 3의 `buildWalls`·`extrusionPaint`·`buildElevatedLines`·`elevatedLineLayout`, Task 4의 `maxZoomFor`
- Produces: `useMyMap` 반환값에 `on3d`, `set3d(bool)`, `exaggeration`, `setExaggeration(x)`, `wallCount`, `elevCount` 추가

- [ ] **Step 1: 구현**

`frontend/src/features/my-map/useMyMap.js`:

import 두 줄:

```js
import { buildWalls, extrusionPaint, buildElevatedLines, elevatedLineLayout } from './lib/kmlWalls.js'
import { maxZoomFor } from './lib/cameraLimit.js'
```

상수를 더한다(`SRC` 근처):

```js
// 기둥과 뜬 경로는 원본과 기하가 다르다(바닥 고리를 되찾고, 갈래를 쪼갠다).
// 그래서 소스를 따로 둔다. 맥케이 파일 기준 43 + 158개뿐이라 부담이 없다.
const WALL_SRC = 'my-map-wall-src'
const WALL_LYR = 'my-map-wall'
const ELEV_SRC = 'my-map-elev-src'
const ELEV_LYR = 'my-map-elev'
const LYR_3D = [WALL_LYR, ELEV_LYR]
```

상태를 더한다:

```js
  const [on3d, setOn3dState] = useState(false)
  const [exaggeration, setExaggerationState] = useState(1)
  const [wallCount, setWallCount] = useState(0)
  const [elevCount, setElevCount] = useState(0)
```

`rebuild` 안, 평면 레이어를 만든 뒤에 3D 소스·레이어를 만든다:

```js
    // 고도 벽과 오르내리는 경로. 평면 도형은 그대로 두므로 "어떤 도형도 숨기지
    // 않는다"는 그대로 지켜진다 — 3D는 그 위에 얹는 것이다.
    const walls = []
    const elevated = []
    for (const fileId of active) {
      const list = byFile.get(fileId)
      if (!list) continue
      for (const w of buildWalls(list)) { w.properties.__file = fileId; walls.push(w) }
      for (const l of buildElevatedLines(list)) { l.properties.__file = fileId; elevated.push(l) }
    }
    setWallCount(walls.length)
    setElevCount(elevated.length)

    const wallData = { type: 'FeatureCollection', features: walls }
    if (map.getSource(WALL_SRC)) map.getSource(WALL_SRC).setData(wallData)
    else map.addSource(WALL_SRC, { type: 'geojson', data: wallData })

    const elevData = { type: 'FeatureCollection', features: elevated }
    if (map.getSource(ELEV_SRC)) map.getSource(ELEV_SRC).setData(elevData)
    // line-z-offset은 line-progress를 쓰므로 lineMetrics가 켜져 있어야 한다.
    else map.addSource(ELEV_SRC, { type: 'geojson', data: elevData, lineMetrics: true })

    const folderFilter = ['in', ['get', '__folder'], ['literal', visibleFolderIds]]
    if (!map.getLayer(WALL_LYR)) {
      map.addLayer({
        id: WALL_LYR, type: 'fill-extrusion', source: WALL_SRC, slot: SLOT,
        filter: folderFilter, paint: extrusionPaint(exaggerationRef.current),
        layout: { visibility: on3dRef.current ? 'visible' : 'none' },
      })
    } else {
      map.setFilter(WALL_LYR, folderFilter)
    }
    if (!map.getLayer(ELEV_LYR)) {
      map.addLayer({
        id: ELEV_LYR, type: 'line', source: ELEV_SRC, slot: SLOT,
        filter: folderFilter, paint: LINE_PAINT,
        layout: { ...elevatedLineLayout(exaggerationRef.current), visibility: on3dRef.current ? 'visible' : 'none' },
      })
    } else {
      map.setFilter(ELEV_LYR, folderFilter)
    }
```

레이어를 지우는 자리(`for (const def of LAYER_DEFS) if (map.getLayer(...)) map.removeLayer(...)`)는 1판에 없다 — 1판은 레이어를 지우지 않고 필터만 바꾼다. 3D도 같은 방식이므로 지우는 코드를 넣지 않는다.

`stateRef`에 3D 상태를 함께 담는다:

```js
  const stateRef = useRef({ activeFileIds, layersByFile, hidden })
  stateRef.current = { activeFileIds, layersByFile, hidden }
  const on3dRef = useRef(on3d)
  on3dRef.current = on3d
  const exaggerationRef = useRef(exaggeration)
  exaggerationRef.current = exaggeration
```

3D 스위치와 과장을 다루는 함수를 더한다(`flyToFolder` 위):

```js
  // 3D는 두 가지가 함께 있어야 보인다: 기둥·뜬 경로 레이어와, 지도를 기울이는 것.
  // 정면에서 내려다보면 기둥이 서 있어도 납작한 면과 구별되지 않는다.
  const set3d = useCallback((on) => {
    setOn3dState(on)
    const map = mapRef.current
    if (!map) return
    for (const id of LYR_3D) {
      if (map.getLayer(id)) map.setLayoutProperty(id, 'visibility', on ? 'visible' : 'none')
    }
    // 과장을 켠 채 가까이 가면 카메라가 기둥 안에 갇힌다. 3D일 때만 한계를 건다.
    map.setMaxZoom(on ? maxZoomFor(exaggerationRef.current) : MAP_CONFIG.maxZoom)
    map.easeTo({ pitch: on ? 60 : 0, duration: 400 })
  }, [mapRef])

  // 높이에만 배수를 건다. 가로는 그대로이므로 위치는 거짓이 되지 않는다.
  const setExaggeration = useCallback((x) => {
    setExaggerationState(x)
    const map = mapRef.current
    if (!map) return
    if (map.getLayer(WALL_LYR)) {
      const paint = extrusionPaint(x)
      map.setPaintProperty(WALL_LYR, 'fill-extrusion-base', paint['fill-extrusion-base'])
      map.setPaintProperty(WALL_LYR, 'fill-extrusion-height', paint['fill-extrusion-height'])
    }
    if (map.getLayer(ELEV_LYR)) {
      map.setLayoutProperty(ELEV_LYR, 'line-z-offset', elevatedLineLayout(x)['line-z-offset'])
    }
    if (on3dRef.current) map.setMaxZoom(maxZoomFor(x))
  }, [mapRef])
```

`MAP_CONFIG`를 import에 더한다:

```js
import { MAP_CONFIG } from '../map/mapConfig.js'
```

반환값에 더한다:

```js
    on3d, set3d, exaggeration, setExaggeration, wallCount, elevCount,
```

- [ ] **Step 2: 문법과 빌드 확인**

```bash
cd frontend && npm run build 2>&1 | tail -2 && npm test 2>&1 | tail -5
```
Expected: 빌드 성공, 시험 전부 통과.

- [ ] **Step 3: 커밋**

```bash
cd /home/john_doe/ProjectAMO && git branch --show-current && git status --short
git add frontend/src/features/my-map/useMyMap.js
git commit -m "feat(my-map): stand the file's altitude shapes up on the main map"
```

---

### Task 6: 3D 스위치와 고도 과장

**Files:**
- Modify: `frontend/src/features/my-map/MyMapPanel.jsx`, `frontend/src/features/my-map/MyMapPanel.css`

**Interfaces:**
- Consumes: Task 5의 `on3d`·`set3d`·`exaggeration`·`setExaggeration`·`wallCount`·`elevCount`, Task 4의 `EXAGGERATION_STEPS`

- [ ] **Step 1: 패널에 더한다**

`frontend/src/features/my-map/MyMapPanel.jsx`:

import에 더한다:

```js
import { Box } from 'lucide-react'
import { EXAGGERATION_STEPS } from './lib/cameraLimit.js'
```

`const { files, ... } = myMap` 에 3D 값을 더한다:

```js
  const { files, activeFileIds, layersByFile, hidden, busy, error, on3d, exaggeration, wallCount, elevCount } = myMap
```

`내 파일` 섹션과 `폴더` 섹션 사이에 3D 섹션을 넣는다:

```jsx
        {activeFiles.length > 0 && (wallCount > 0 || elevCount > 0) && (
          <section className="my-map-section">
            <div className="layer-tile-group-title">3D</div>
            <button
              type="button"
              data-testid="my-map-3d"
              className={`my-map-3d-toggle${on3d ? ' is-active' : ''}`}
              aria-pressed={on3d}
              onClick={() => myMap.set3d(!on3d)}
            >
              <span className="my-map-mark" aria-hidden="true">{on3d && <Check size={12} strokeWidth={3} />}</span>
              <Box size={14} strokeWidth={2} aria-hidden="true" />
              <span>공역 기둥 세우고 지도 기울이기</span>
              <span className="my-map-count">{(wallCount + elevCount).toLocaleString()}</span>
            </button>
            {on3d && (
              <label className="my-map-exag">
                {'고도 과장 '}
                <select
                  data-testid="my-map-exaggeration"
                  value={exaggeration}
                  onChange={(e) => myMap.setExaggeration(Number(e.target.value))}
                >
                  {EXAGGERATION_STEPS.map((x) => <option key={x} value={x}>{x}배</option>)}
                </select>
                <span className="my-map-exag-hint">
                  실제 비율로는 반경 9km 관제권의 높이가 300m라 납작합니다. 5~10배가 보기 좋습니다.
                </span>
              </label>
            )}
          </section>
        )}
```

- [ ] **Step 2: 모양**

`frontend/src/features/my-map/MyMapPanel.css` 끝에 더한다:

```css
.my-map-3d-toggle {
  display: flex; align-items: center; gap: 8px;
  width: 100%; padding: 9px 10px;
  border: 1.5px solid rgba(148, 163, 184, 0.4);
  border-radius: 12px;
  background: var(--bg-1); color: inherit;
  font-family: inherit; font-size: 13px; text-align: left; cursor: pointer;
  transition: border-color 120ms ease, background 120ms ease, color 120ms ease;
}
.my-map-3d-toggle.is-active {
  border-color: var(--accent);
  background: color-mix(in srgb, var(--accent) 8%, var(--bg-1));
  color: var(--accent);
}
.my-map-exag { display: block; margin-top: 8px; font-size: 12.5px; color: var(--text-2); }
.my-map-exag select {
  margin-left: 4px; padding: 4px 8px;
  border: 1.5px solid rgba(148, 163, 184, 0.4); border-radius: 8px;
  background: var(--bg-2); color: inherit; font-family: inherit; font-size: 12.5px;
}
.my-map-exag-hint { display: block; margin-top: 6px; font-size: 11.5px; color: var(--text-3); line-height: 1.5; }
```

- [ ] **Step 3: 빌드와 전체 시험**

```bash
cd frontend && npm run build 2>&1 | tail -2 && npm test 2>&1 | tail -5
```
Expected: 빌드 성공, 시험 전부 통과.

- [ ] **Step 4: 눈으로 확인**

맥케이 파일을 올리고 사용자에게 넘긴다:

> 1. `3D` 칸이 보이나요? (벽 43 + 경로 158 = 201로 나와야 합니다)
> 2. 켜면 지도가 기울고 공역이 기둥으로 서나요?
> 3. 고도 과장을 10배로 올리면 관제권의 2층 구조가 보이나요?
> 4. **10배로 켠 채 확대를 끝까지 해보세요** — 기둥 안에 갇히나요, 아니면 어느 선에서 더 안 들어가나요?
> 5. 3D를 끄면 지도가 다시 평평해지고 확대 한계도 풀리나요?

**4번이 이 판의 핵심입니다.** 갇히면 `cameraLimit.js`의 표를 조정한다 — 그 값은 눈으로 맞추는 값이다.

- [ ] **Step 5: 커밋**

```bash
cd /home/john_doe/ProjectAMO && git branch --show-current && git status --short
git add frontend/src/features/my-map/MyMapPanel.jsx frontend/src/features/my-map/MyMapPanel.css
git commit -m "feat(my-map): add the 3D switch and altitude exaggeration"
```

---

### Task 7: 계약과 등록부

**Files:**
- Modify: `frontend/verification/contracts/my-map.spec.mjs`
- Modify: `frontend/test/fixtures/my-map/folders.kmz` (3D·설명 재료 추가)
- Modify: `docs/policies/verification/contracts.md`

- [ ] **Step 1: 시험용 파일에 재료를 더한다**

지금 파일에는 설명도 아이콘도 고도도 없다. 다시 만든다:

```bash
cd /home/john_doe/ProjectAMO/frontend && node -e '
const { deflateRawSync, crc32 } = require("node:zlib")
const fs = require("node:fs")
const kml = `<?xml version="1.0" encoding="UTF-8"?>
<kml xmlns="http://www.opengis.net/kml/2.2"><Document><name>계약용</name>
<Folder><name>RKTA TAEAN</name>
  <Placemark><name>공항</name><description>계약용 설명글</description>
    <Style><IconStyle><Icon><href>http://maps.google.com/mapfiles/kml/paddle/A.png</href></Icon></IconStyle></Style>
    <Point><coordinates>126.42,36.72,0</coordinates></Point></Placemark>
  <Folder><name>출항절차</name>
    <Placemark><name>WP1</name><LineString><coordinates>126.42,36.72,0 126.55,36.85,600</coordinates></LineString></Placemark>
  </Folder>
</Folder>
<Folder><name>공역</name>
  <Placemark><name>R77</name><Polygon><outerBoundaryIs><LinearRing><coordinates>
    127.0,37.0,0 127.4,37.0,0 127.4,37.4,0 127.0,37.4,0 127.0,37.0,0
  </coordinates></LinearRing></outerBoundaryIs></Polygon></Placemark>
</Folder>
<Folder><name>고도 벽</name>
  <Placemark><name>테스트 벽</name><MultiGeometry>
    <Polygon><outerBoundaryIs><LinearRing><coordinates>
      127.0,37.0,1524 127.0,37.0,304.8 127.1,37.0,304.8 127.1,37.0,1524
    </coordinates></LinearRing></outerBoundaryIs></Polygon>
    <Polygon><outerBoundaryIs><LinearRing><coordinates>
      127.1,37.0,1524 127.1,37.0,304.8 127.1,37.1,304.8 127.1,37.1,1524
    </coordinates></LinearRing></outerBoundaryIs></Polygon>
    <Polygon><outerBoundaryIs><LinearRing><coordinates>
      127.1,37.1,1524 127.1,37.1,304.8 127.0,37.0,304.8 127.0,37.0,1524
    </coordinates></LinearRing></outerBoundaryIs></Polygon>
  </MultiGeometry></Placemark>
</Folder>
</Document></kml>`
const name = Buffer.from("doc.kml")
const body = Buffer.from(kml, "utf8")
const comp = deflateRawSync(body)
const crc = crc32(body)
const loc = Buffer.alloc(30)
loc.writeUInt32LE(0x04034b50, 0); loc.writeUInt16LE(20, 4); loc.writeUInt16LE(8, 8)
loc.writeUInt32LE(crc, 14); loc.writeUInt32LE(comp.length, 18); loc.writeUInt32LE(body.length, 22)
loc.writeUInt16LE(name.length, 26)
const cen = Buffer.alloc(46)
cen.writeUInt32LE(0x02014b50, 0); cen.writeUInt16LE(20, 6); cen.writeUInt16LE(8, 10)
cen.writeUInt32LE(crc, 16); cen.writeUInt32LE(comp.length, 20); cen.writeUInt32LE(body.length, 24)
cen.writeUInt16LE(name.length, 28)
const cenOffset = loc.length + name.length + comp.length
const eocd = Buffer.alloc(22)
eocd.writeUInt32LE(0x06054b50, 0); eocd.writeUInt16LE(1, 8); eocd.writeUInt16LE(1, 10)
eocd.writeUInt32LE(cen.length + name.length, 12); eocd.writeUInt32LE(cenOffset, 16)
fs.writeFileSync("test/fixtures/my-map/folders.kmz", Buffer.concat([loc, name, comp, cen, name, eocd]))
fs.writeFileSync("test/fixtures/my-map/folders.kml", body)
console.log("wrote", fs.statSync("test/fixtures/my-map/folders.kmz").size, "bytes")
'
```

폴더가 3개 → 4개가 되므로 **기존 계약의 기대값을 함께 고쳐야 한다.** `folders.kmz`를 쓰는 단언 중 도형 수를 세는 것(`drawnCount`)은 지점 1 + 선 1 + 면 1 + 벽 1 = **4**가 된다.

- [ ] **Step 2: 기존 계약의 기대값 갱신**

시험용 파일이 폴더 3개·도형 3개에서 폴더 4개·도형 4개가 되므로 기존 단언을 고친다.

- 「파일을 올리면…」 — `sourceCount`·`drawnCount`가 `toBe(3)` → **`toBe(4)`**. 접힌 최상위에 `고도 벽`이 하나 늘었으니 `await expect(tree.getByText('고도 벽', { exact: true })).toBeVisible()`를 더한다.
- 「폴더를 끄면…」 — 처음 `toBe(3)` → **`toBe(4)`**, `공역`을 끈 뒤 `toBe(2)` → **`toBe(3)`**.
- 「상위 폴더를 끄면…」 — 처음 `toBe(3)` → **`toBe(4)`**, `RKTA TAEAN`을 끈 뒤 `toBe(1)` → **`toBe(2)`** (`공역`의 면 + `고도 벽`의 벽이 남는다).
- 「찾기에 이름을 치면…」 — 끝의 `toBe(3)` → **`toBe(4)`**.

바꾼 뒤 `npm --prefix frontend run dev:contract:fast -- contracts/my-map.spec.mjs`로 먼저 데스크톱만 돌려 숫자가 맞는지 확인한다.

- [ ] **Step 3: 계약 추가**

`frontend/verification/contracts/my-map.spec.mjs` 끝의 `test.describe` 안에 더한다:

```js
  test('아이콘이 있는 지점은 아이콘으로, 없는 지점은 원으로 그린다', async ({ page }) => {
    await openMyMap(page)
    await loadFixture(page)

    // 아이콘이 올라간 지점에는 __icon이 심긴다.
    await expect.poll(() => page.evaluate(() => {
      const src = window.__map?.getSource('my-map-src')
      const features = src?.serialize().data.features ?? []
      return features.filter((f) => f.properties?.__icon).length
    }), { timeout: 30000 }).toBe(1)

    // 원 레이어는 아이콘이 붙은 지점을 걸러낸다.
    const circleFilter = await page.evaluate(() => JSON.stringify(window.__map?.getFilter('my-map-circle')))
    expect(circleFilter).toContain('__icon')
  })

  test('도형을 누르면 파일에 적힌 설명이 글자로 뜬다', async ({ page }) => {
    await openMyMap(page)
    await loadFixture(page)

    // 지점 자리를 화면 좌표로 바꿔 누른다.
    const point = await page.evaluate(() => {
      const p = window.__map.project([126.42, 36.72])
      const box = window.__map.getCanvas().getBoundingClientRect()
      return { x: box.x + p.x, y: box.y + p.y }
    })
    await page.mouse.click(point.x, point.y)
    await expect(page.locator('.my-map-popup')).toBeVisible()
    await expect(page.locator('.my-map-popup-title')).toHaveText('공항')
    await expect(page.locator('.my-map-popup-body')).toHaveText('계약용 설명글')
  })

  test('3D를 켜면 기둥과 뜬 경로가 보이고 확대 한계가 걸린다', async ({ page }) => {
    await openMyMap(page)
    await loadFixture(page)

    const before = await page.evaluate(() => ({
      wall: window.__map?.getLayoutProperty('my-map-wall', 'visibility') ?? null,
      maxZoom: window.__map?.getMaxZoom(),
      pitch: Math.round(window.__map?.getPitch() ?? 0),
    }))
    expect(before.wall).toBe('none')
    expect(before.pitch).toBe(0)

    await page.getByTestId('my-map-3d').click()
    await expect.poll(() => page.evaluate(() => window.__map?.getLayoutProperty('my-map-wall', 'visibility'))).toBe('visible')
    await expect.poll(() => page.evaluate(() => Math.round(window.__map?.getPitch() ?? 0))).toBeGreaterThan(30)

    // 과장을 올리면 더 물러나야 한다 — 안 그러면 기둥 안에 갇힌다.
    await page.getByTestId('my-map-exaggeration').selectOption('10')
    await expect.poll(() => page.evaluate(() => window.__map?.getMaxZoom())).toBeLessThanOrEqual(9)

    // 끄면 평평해지고 한계도 풀린다.
    await page.getByTestId('my-map-3d').click()
    await expect.poll(() => page.evaluate(() => window.__map?.getLayoutProperty('my-map-wall', 'visibility'))).toBe('none')
    await expect.poll(() => page.evaluate(() => window.__map?.getMaxZoom())).toBe(before.maxZoom)
  })
```

- [ ] **Step 4: 계약 실행**

포트가 비어 있어야 한다. 다른 세션이 쓰고 있으면 **끄기 전에 사용자에게 확인한다.**

```bash
ss -ltnp 2>/dev/null | grep -E ':3001|:5173' || echo "포트 비었음"
cd /home/john_doe/ProjectAMO && npm run dev:contract -- --grep my-map
```
Expected: desktop·iPad landscape 전부 통과, mobile skip. 실패하면 `superpowers:systematic-debugging`으로 원인을 찾는다.

- [ ] **Step 5: 등록부 갱신**

`docs/policies/verification/contracts.md`의 `my-map` 줄에서 다루는 것과 상태를 갱신한다 — 아이콘·설명 풍선·3D가 더해졌고 시험용 파일에 벽과 설명이 들어갔다는 것을 적는다. 날짜는 실제로 통과시킨 날로 적는다.

- [ ] **Step 6: 전체 확인과 커밋**

```bash
cd frontend && npm test 2>&1 | tail -5 && npm run build 2>&1 | tail -2
cd /home/john_doe/ProjectAMO && graphify update .
git branch --show-current && git status --short
git add frontend/verification/contracts/my-map.spec.mjs frontend/test/fixtures/my-map docs/policies/verification/contracts.md
git commit -m "test(my-map): cover icons, descriptions and 3D"
```

---

## 완료 기준

- `cd frontend && npm test` 전부 통과 (1판 것 + 신규: 아이콘 7, 페인트 3, 카메라 5, 옮겨온 벽 19)
- `cd frontend && npm run build` 성공
- `npm run dev:contract -- --grep my-map` 전부 통과 (desktop·iPad, mobile skip)
- 맥케이 파일로 눈 확인:
  - 지점이 압정·깃발 모양으로 나오고, 아이콘 없는 것만 동그라미
  - `물찻오름`을 누르면 설명이 뜬다
  - 3D를 켜면 공역 43개가 기둥으로 서고 경로 158개가 뜬다
  - **10배로 켠 채 끝까지 확대해도 기둥 안에 갇히지 않는다**
