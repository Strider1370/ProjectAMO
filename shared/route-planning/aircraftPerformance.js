// Defaults only. User storage/preferences belong to the browser, never the planner.
export const DEFAULT_PERFORMANCE_BY_RULE = {
  IFR: { tasKt: 450, altitudeFt: 31000 },
  VFR: { tasKt: 120, altitudeFt: 5500 },
}
